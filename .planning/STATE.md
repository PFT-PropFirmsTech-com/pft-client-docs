# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-30)

**Core value:** Funded traders rank + compete in monthly prize pool competitions. Affiliates see per-purchase commission breakdown; admins reconcile commissions in Payment History CSV.
**Current focus:** v1.1 milestone complete — planning next milestone (`/gsd:new-milestone`)

## Current Position

Phase: 7 — Used Margin Display (ad-hoc, post-v1.1) — COMPLETE
Plan: 07-02 COMPLETE — backend marginUsage response shape + MarginUsageCard mounted in TradingDashboardShared; human-verify DEFERRED
Status: 07-02 committed pft-backend `1a7aa01e` (1 file, +13) + pft-dashboard `1acd03c6` (4 files, +221/-1); both PUSHED to origin/main-2026; NOT deployed
Last activity: 2026-06-30 — 07-02 widget shipped after Blocker 1+2 revision (orphan AccountInfoSection bypass + accountStatistics.service.ts response shape extension)

Progress: v1.0 [██████████] 100% (10/10) · v1.1 [██████████] 100% (4/4) · Phase 4.1 [██████████] 100% (1/1) · Phase 5 [██████████] 100% (1/1) · Phase 6 [██████████] 100% (1/1) · Phase 7 [██████████] 100% (2/2 — persistence 07-01 + widget 07-02)

**Open across both milestones (post-deploy):** v1.0 live human-verify (Phases 2 & 3 — anon masking, opt-out, competition close, cache isolation) + v1.1 live human-verify (Phase 4 — Purchase Report card render, Export CSV downloads, showOverview-only scoping, 6 admin CSV cols populated, single-POST network pattern). All gated on next main-2026 deploy. Tracked in phase SUMMARYs.

07-02: Used Margin Display dashboard widget — closes ticket cmovizb320007qs0k0fue250p (TC BACKLOG) end-to-end. EXECUTION OUTCOME: original plan halted at Task 0 with two blockers (`07-02-BLOCKER.md`); revised + re-executed in this session. **Blocker 1 (resolved):** `strict:false` on AccountRuleState model is necessary but NOT sufficient — `accountStatistics.service.ts:462-487` cherry-picks fields into the response DTO, so the new 07-01 fields were silently dropped end-to-end. Audit also identified `Statistics/statistics.service.ts:232-255` `.select({...})` whitelist + `User/user.service.ts:439-454` `.select({...})` whitelist as additional read-path drop points; deliberately NOT touched — only the single dashboard widget read path needed extension. Fix: +13 lines on `accountStatistics.service.ts` cherry-picking `marginUsage:{current,peak}` from the already-fetched lean `ruleState` doc with `typeof === "number"` guards (no extra Mongo query, no NaN/Infinity escape). **Blocker 2 (resolved):** `AccountInfoSection.tsx` is an ORPHAN — imported at TradingDashboardShared.tsx:81 but never JSX-rendered (`grep "<AccountInfoSection" pft-dashboard/src/` returns 0 matches). Plan's "single AccountInfoSection edit lights up both routes" mechanism does not exist today. Fix: mount `MarginUsageCard` directly inside `TradingDashboardShared.tsx` as a new `<section className="mb-8">` ABOVE the existing CompactInfoCards section (line ~2095); AccountInfoSection left orphaned (pre-existing condition, out of scope). Single component renders identically on BOTH `/accounts/[id]/statistics/[mtacc]` (client) and `/admin/users/.../account/[accountId]` (admin) — both routes mount TradingDashboardShared. NO role conditional. WIRING: `currentMarginUsedPct = accountInfo.margin / accountInfo.equity * 100` (NOT `accountInfo.marginLevel` — that's the INVERSE per RESEARCH §Pitfall 1) with `typeof === "number" && > 0` guards on BOTH legs → null otherwise; `peakMarginUsedPct = statisticsDataFromHook.marginUsage.peak` from new backend response shape (`(as any)` cast because hook's `data` is typed `any`, contract documented via new `MarginUsageDTO` in types.ts) with `> 0` guard so fresh-account 0 renders "—" not "0.0%". **MarginUsageCard component** (173 lines, new): div-card visual mirror of ConsistencyScoreCard (NOT shadcn Card — programs-details folder convention), big bold current % (3xl bold), color-graded progress bar (manual `<div role="progressbar">` — no Progress primitive needed), peak label below ("Peak: X.X%" or "—"), Info tooltip explaining "margin / equity × 100 + all-time peak"; icon flip ShieldCheck → AlertTriangle at ≥80%; color thresholds emerald <50% / amber 50-80% / red ≥80% (mirrors ConsistencyScoreCard palette: `#10b981`/`#f59e0b`/`#ef4444`); `useMemo`-wrapped rating computation with `Number.isFinite` guards + `>=0`/`>0` rejections so 0 + null + NaN + Infinity ALL render "—"; `Math.max(0, Math.min(100, pct))` clamp on bar width so overflows can't render. **i18n**: 5 keys added to `dashboard.programDetails.marginUsage` namespace in en.json (title/current/peak/tooltip/noPositions). JA deliberately NOT updated (falls back to en, matches 02-02/03-03/03-04/04-04 convention). **types.ts**: new `MarginUsageDTO {current: number | null; peak: number | null}` interface documenting backend response contract. **Files modified** (1 backend + 4 dashboard, EXACTLY): `pft-backend/.../AccountStatistics/accountStatistics.service.ts` + `pft-dashboard/.../programs-details/{MarginUsageCard.tsx (NEW), TradingDashboardShared.tsx, types.ts}` + `pft-dashboard/messages/en.json`. **NOT touched**: `statistics.service.ts`, `user.service.ts`, `AccountInfoSection.tsx` (orphan), `pfr-super-admin`, `pft-rule-checker` (already shipped via 07-01 `abede27`). Pre-edit `git fetch origin main-2026` discovered 1 incoming pft-backend commit (`6689f3d1` — Klaviyo PAP-recovery event-tracking, zero overlap with target file); fast-forwarded clean. Dashboard already up to date. tsc scoped on all 4 touched files = 0 new errors (pre-existing baseline preserved). 2 atomic commits: pft-backend main-2026 `1a7aa01e` (1 file +13) + pft-dashboard main-2026 `1acd03c6` (4 files +221/-1); BOTH PUSHED (`6689f3d1..1a7aa01e` + `5553b7e0..1acd03c6`); NOT deployed. **Deploy ordering (strict)**: pft-rule-checker (07-01 `abede27`, already pushed) → pft-backend (`1a7aa01e`) → pft-dashboard (`1acd03c6`). Reverse order = "—" rendered everywhere until backend catches up. Human-verify DEFERRED to post-deploy (matches 04-04 / 05-01 / 06-01 convention): on live TC funded account verify (1) BOTH client + admin routes render MarginUsageCard identically (no role divergence), (2) current % updates via socket as positions open/close, (3) peak ratchets up only — closing all positions keeps peak, next EOD/daily reset keeps peak (07-01 audit verified peakMarginUsedPercent absent from eodService.ts / dailyResetScheduler.ts / resetDailyValues / payoutCycleReset.service.ts), (4) no positions / fresh account renders "—" not "0.0%" / NaN% / Infinity%, (5) tooltip explains margin/equity*100 + all-time peak. Bug signatures to flag: current % shows ~350% when expected ~5% = marginLevel/marginUsed flip; peak resets to 0 on EOD = 07-01 reset audit gap; client vs admin visual divergence = role-conditional regression. Ticket workflow: screenshot lit card → post to `cmovizb320007qs0k0fue250p` → WAITING_CLIENT. OPS FOLLOW-UP (NOT this plan, NOT code): Trading Cult Super Admin per-brand `pagePermissions` for `/admin/risk-intelligence/*`, `/admin/users/suspicious-accounts`, `/admin/users/fraud-check` must permit `backOffice` role — flag for Bob per `reference_page_visibility_permissions.md` memory + RESEARCH §Pitfall 5. `07-02-BLOCKER.md` kept in place for historical reference (Task 0 audit record).

07-01: Used Margin Display persistence backbone from ticket cmovizb320007qs0k0fue250p (TC BACKLOG). Added `currentMarginUsedPercent` (per-tick snapshot) + `peakMarginUsedPercent` (all-time Math.max ratchet) to `pft-rule-checker` `accountrulestates` — mirrors the proven `peakTotalDrawdownPercent` pattern beat-for-beat. Schema (`accountRuleState.model.ts` lines 48-49: `Number, default 0`, no index) + TS interface (`accountRuleState.interface.ts` lines 108-112) + service (`ruleStateService.ts` `updateAfterSync`: Math.max ratchet block inserted after existing peak-block at ~lines 524-534 reading `accountInfo.margin` directly off the destructured `AccountSyncData.accountInfo`, with guards `currentEquity > 0 && typeof marginValue === "number" && marginValue > 0` → 0 otherwise so no NaN/Infinity; written to the SINGLE downstream `updateFields` $set at lines 672-673; payout-reset block at line ~1086 left peakMarginUsedPercent OUT with inline comment `// peakMarginUsedPercent is all-time, NOT reset on payout/daily`). DEVIATION FROM PLAN: plan referenced lines ~635/658/717 for multiple $set blocks — only ONE $set exists in `updateAfterSync` (line 658, the `updateFields` object); no further downstream writes — single insertion sufficed. Denominator decision LOCKED: `margin / equity * 100` (NOT MT5 marginLevel = `equity/margin*100`) per RESEARCH §Pitfall 1. EOD-RESET AUDIT (all paths verified peak preserved): (1) `ruleStateService.ts` payout-reset block — explicitly excludes peakMarginUsedPercent + carries inline comment; (2) `eodService.ts` — grep `peakMarginUsedPercent` returns 0 matches (only `peakDailyDrawdownPercent` + `peakTrailingDrawdownPercent` reset at lines 373-374); (3) `dailyResetScheduler.ts` — grep clean 0 matches; (4) `accountRuleState.model.ts` `resetDailyValues` static (Phase 1+2 update pipelines, lines 403-404, 481-482) — only resets daily/trailing peaks, NEVER peakTotalDrawdownPercent NOR peakMarginUsedPercent (confirms the symmetry); (5) NEW remote file `payoutCycleReset.service.ts` (landed in the ea8f358 fast-forward) — grep clean 0 peak refs, no conflict. PFT-BACKEND ZERO CHANGE: confirmed via `grep -n "strict: false" pft-backend/src/app/models/accountRuleState.model.ts` → `16:const accountRuleStateSchema = new Schema({}, { strict: false });` — passthrough surfaces new fields automatically when rule-checker writes them; no model edit, no DTO change, no migration. NO RETROACTIVE BACKFILL: peakMarginUsedPercent populates from rule-checker deploy date forward; dashboard widget (07-02) will read 0 until at least one tick of `updateAfterSync` lands post-rollout. DEPLOY ORDERING: rule-checker MUST deploy FIRST before 07-02 widget goes live. Pre-edit `git fetch origin main-2026` discovered 7 new remote commits (pay-after-pass Klaviyo work + event-driven V2 + `payoutCycleReset.service.ts` + funded-queue orphan recovery) since plan-write per `feedback_rebase_when_remote_already_fixed.md`; fast-forwarded `1b3157b..ea8f358` clean before editing — `payoutCycleReset.service.ts` audited above (zero peak refs, no clash). SANITY MATH: equity=10000/margin=2500 → 25.0; equity=0 → 0; margin=undefined → 0; equity=-100 → 0; margin=0 → 0 — all guarded, no NaN/Infinity. tsc clean on all 3 touched files. Commit pft-rule-checker main-2026 `abede27` (3 files, +25/-2); PUSHED (`ea8f358..abede27`); NOT deployed. Human-verify DEFERRED to post-deploy (matches 06-01/05-01/04.1-01 convention): mongosh `db.accountrulestates.findOne({updatedAt:{$gt:new Date(Date.now()-60000)}})` confirms both new fields present + `peakMarginUsedPercent >= currentMarginUsedPercent`; compare `currentMarginUsedPercent` against MT5 terminal `(margin/equity)*100` to 1 decimal on a live-positions funded acct; close all positions → current drops to 0, peak stays (ratchet); force daily reset / next EOD → peak UNCHANGED. OPS FOLLOW-UP (NOT this plan, NOT this repo): Risk Intelligence backoffice page visibility = Super Admin per-brand `pagePermissions` check (pfr-super-admin Permissions tab) per `reference_page_visibility_permissions.md` — flag for Bob, NOT a code change. Source ticket cmovizb320007qs0k0fue250p (Trading Cult BACKLOG).

06-01: Funded Queue Ready Badge from ticket cmqt9rtjl002rny0kkawu1c6y (TC HIGH). EXECUTION OUTCOME: closed-by-remote, ZERO new commits authored. Pre-edit `git fetch origin main-2026` discovered another dev (Claude Opus 4.8 / abu jobayer) pushed two commits ~minutes before execution started: pft-backend `c8340316 feat(funded-queue): expose eligibleManualApproval count in stats` (+9/-1 controller only — extends existing /funded-queue/stats response with `eligibleManualApproval` field, NOT a new /ready-count route) + pft-dashboard `73810f47 feat(sidebar): red dot on Funded Queue when entries await manual approval` (+30/-1 in useAdminSidebarPending.ts + +2 in useFundedQueue.ts — adds 5th useQuery sharing the `["funded-queue-stats"]` cache key with the existing Funded Queue page hook). Both nested repos fast-forwarded clean. DEFERRED per Rule 4 + 04.1-01 precedent + `feedback_rebase_when_remote_already_fixed.md` memory. REMOTE vs PLAN ARCHITECTURAL DELTAS: (1) Remote extends `/funded-queue/stats` instead of dedicated `/ready-count` route — leaner, no new auth wiring, reuses cache; (2) Remote uses STORED `reason: "manual_approval_pending"` field via `countDocuments({status:"pending", reason:"manual_approval_pending"})` instead of plan's LIVE-COMPUTE Kyc+Contract joins — accepts ≤5min cron-lag staleness that plan's research §Pitfall 1 explicitly flagged; (3) Remote OMITS the `funded_queue_settings.manualApprovalEnabled` toggle gate — count fires regardless of brand setting (plan's research §Pitfall 3 warned this lights dots on auto-process brands where rows are about to vanish; TC has manualApprovalEnabled=true so source-ticket brand unaffected, but a real regression on auto-process brands); (4) Remote covers only `/admin/funded-queue` leaf in `adminPendingForHref` — relies on SidebarSubmenu's existing auto-propagation for `/admin/programs` parent inheritance (plan was belt-and-suspenders explicit); (5) Remote shares cache key `["funded-queue-stats"]` with existing `useFundedQueueStats` page hook (one request, two surfaces) vs plan's independent `["admin", "fundedQueueReadyCount"]` key. Field name: remote `pendingFundedApproval`, plan was `pendingFundedManualApproval`. Cadence (5min) + auth (admin+backOffice via existing /stats route) MATCH plan. Protected files (SidebarItem / SidebarSubmenu / sidebar-config / NotificationsProvider) UNTOUCHED by remote — verified via diff. No commits to pfr-super-admin or pft-rule-checker. Both pushed to `origin/main-2026`; NOT deployed. Human-verify DEFERRED (matches 04-04 / 05-01 / 04.1-01 convention) — post-deploy verify the REMOTE shape: curl `/funded-queue/stats` returns `eligibleManualApproval`, synthetic-seed mongosh needs to force `reason: "manual_approval_pending"` (cron-dependent), TC sidebar lights, BackOffice no 403, ~5min Network cadence, parent-inherits-dot via SidebarSubmenu. Open follow-ups (DEV ticket candidates): (1) MEDIUM — toggle-gate missing on auto-process brands, (2) LOW — stored-field 5min staleness Pitfall 1 confirmed; (3) LOW — verify parent inheritance works as remote commit message claims. Ticket workflow: screenshot lit dot on TC → reply ticket cmqt9rtjl002rny0kkawu1c6y → WAITING_CLIENT.

04.1-01: v1.1 reopen-patch from ticket cmqqchwh500bspi0kxw23o2rl, 3 affiliate-reporting bugs from `.planning/v1.1-MILESTONE-AUDIT.md`. EXECUTION OUTCOME: only Bug 1 needed a new commit; Bugs 2 + 3 already closed on `origin/main-2026` by 4 hotfix commits that landed between plan-write and plan-execute (`7110ce54` crash guard + accountSize label fix, `bc0ef4ec` purchase amount no-zero, `866c7b60` JA i18n, `31ab6d1b` USD-canonical Purchase Report). Bug 1 (`pft-dashboard/src/hooks/usePayments.ts` `useExportPaymentsCsv` admin Payment History CSV) — tier-1-only `c?.amount` replaced with `commissions.reduce((sum,c)=>sum+(Number(c?.amount)||0),0)` SUM across all MLM tiers; tier-1 (`directTier`) still owns rate column + Affiliate identity columns (User ID/Name/Email/Currency); blank-fallback when `commissions.length===0` so non-MLM payments don't show "0"; header renamed "Commission Rate (%)" → "Direct Commission Rate (%)" to disambiguate the single-rate column from the multi-tier-sum amount column. Bug 2 SOLVED differently on remote (`31ab6d1b`): instead of plan's row-currency override (`formatCurrency(amount, r.currency)`), remote forces USD-canonical render — `formatPurchaseRowCurrency(r.amount, "USD")` for commission cell + `r.payment?.usdAmount ?? (r.amount/(r.rate/100))` for purchase cell + `purchaseCurrency = "USD"` — so row is arithmetically self-consistent (purchase × rate% == commission) and never shows ¥1/¥3 for USD-stored commissions on JPY brand. Bonus: remote also adds reversed-commission UI (line-through + "Reversed" tag). Bug 3 SOLVED differently on remote (`7110ce54`): instead of plan's regex-based `parseAccountSize` (`/^([\d.]+)\s*([kKmM]?)/` with k=1e3/M=1e6 multipliers mirroring pft-backend accountSize.utils.ts), remote adds `formatAccountSizeLabel(size)` — strip-commas-then-Number with raw-string fallback (`$5k` literal) — handles "$200,000"/"25k"/"200k"/"5000". Same audit outcome (no $NaN); different shape. `formatChallengeType` util does NOT exist in `src/utils`/`src/lib` (grep clean) — challenge type stays raw camelCase (`twoStep`) per plan's explicit instruction. DEVIATION HANDLING: initial commit `80e4157f` carried plan's Bug 1 + Bug 2 + Bug 3 shape; rebase against fetched remote conflicted in `AffiliatesContainer.tsx`; aborted rebase, soft-reset, `git checkout --` on AffiliatesContainer, fast-forwarded remote, re-committed Bug 1 only as `60e9b37c` — honors Rule 4 (defer to deployed-style remote architecture on Bugs 2/3). Diff: 1 file, +17/-10. tsc 259-baseline preserved; 0 new errors at touched lines. v1.1 audit (status `gaps_found`, 1/3 satisfied) now fully closed across the 3 commits. PUSHED `31ab6d1b..60e9b37c`; NOT deployed. Human-verify DEFERRED (matches 04-04 / 03-04 / 03-03 / 02-02 / 05-01 convention) — post-deploy on Trading Cult: as `tedija8928`-class MLM affiliate verify Purchase Report Commission Amount renders USD ($1.62 not ¥1) + Product `twoStep $5,000` not `twoStep $NaN` + Purchase Amount in USD; as admin verify Payment History CSV Commission Amount column = manual sum of bulk-by-orders response per orderId + header "Direct Commission Rate (%)"; regress-check Withdrawals/Referrals/Payout-History/single-POST network pattern. Ticket workflow: screenshot Purchase Report → post to `cmqqchwh500bspi0kxw23o2rl` → flip status WAITING_CLIENT.

05-01: Daily P&L Calendar undercount fix on Trading Cult account 13535 / 2026-06-18 (widget rendered "+$55" instead of ~$20). ROOT CAUSE: `mergedFromDeals` in `pft-dashboard/src/hooks/useTradingDashboardData.ts` close-deal branch (entry==="1") did `if (remain <= 0 || totalOpen <= 0) continue;` — silently dropping every CLOSE deal whose matching OPEN was outside the loaded buffer (paginated load / archive boundary). DB sum: 12 matched closes $54.85 + 1 orphan (Deal 1036327 / Position 1048884, Profit -$34.69) = true $20.16; widget rendered `Math.abs(54.85).toFixed(0)` → "+$55". GOTCHA: the `Math.abs` makes this class of bug visually invisible — inflated-positive looks like a normal positive day, so negative-day spotting won't catch future regressions; the diff-scope assertion in the commit is the durable safeguard. FIX: split the combined guard — when `totalOpen <= 0` (orphan) emit ONE synthetic closed-position row from the close deal alone (mirrors `emitClosed` field shape: Profit/Commission/Storage/Symbol/Volume/Action from close, OpenPrice from PricePosition fallback Price, ClosePrice from Price, OpenTime===CloseTime===tSec, `DurationMs:1` — CRITICAL because `isDisplayableMergedTrade` at `utils/tradeHistoryDisplay.ts:119` drops rows when `durationSec===0 && durationMs===0`, and openTime===closeTime makes durationSec=0 so durationMs MUST be truthy; OrderOpen="", OrderClose=close-deal-Order, TP/SL from close deal, `_orphanClose:true` debug marker), then `continue`; kept the matched-pair `while` loop UNCHANGED. DECISION: kept `mergedFromDeals` inline in the closure (no refactor — out of plan scope); did NOT add a test file (pft-dashboard has no jest/vitest; standing up infra for one bug = scope creep). Diff: `1 file changed, 44 insertions(+), 1 deletion(-)` — entry==="0", entry==="2", and the matched-pair while-loop inside entry==="1" all byte-identical to before. tsc on changed file shows 11 pre-existing module-resolution errors + 1 pre-existing MapIterator/--downlevelIteration error at line 1065 (all consistent with 259 pre-existing project errors per memory); zero new errors at patched lines. Commit pft-dashboard main-2026 `ba06d755`, PUSHED (`73414998..ba06d755` — required rebase against 73414998 first, single-commit clean). NOT deployed; human-verify DEFERRED to post-deploy (matches 04-04 / 03-04 / 03-03 / 02-02 convention) — resume signal: ~$20 cell on Trading Cult account 13535 / 2026-06-18 + screenshot to ticket `cmquy9bqo005pny0kw6j0lr71` → status WAITING_CLIENT. Deploy note: pft-dashboard main-2026 → all brands ship together; bug is data-shape-driven (orphan close on archive boundary), not brand-specific. OUT-OF-SCOPE follow-ups (DO NOT block this fix): (1) MT5 broker shows $9.90 for 2026-06-18 while DB sum is $20.16 — separate data-sync investigation, recommend DEV ticket once sample of accounts gathered; (2) `pft-rule-checker` likely has the symmetric server-side pairing orphan-drop bug — if rule-checker undercounts losses by the same mechanism, drawdown/daily-loss enforcement is undercounting; recommend DEV ticket to audit + port the same fix server-side. Ticket: `cmquy9bqo005pny0kw6j0lr71`.

04-04: Affiliate Overview page now ships a "Purchase Report" card below "Your Referrals" (above Payout History) inside the showOverview branch only — NOT on the Withdrawals view. Pulls from the GET /affiliates/my-commissions route shipped by 04-01 (Auth userRole.user, server-scoped to req.user._id). MLM/Hybrid affiliates get Tier 1/2/3 tabs (mirrors the existing Referrals tabs using `tigerTab + tabHoverTeal + tabActiveTeal` theme classes); Standard affiliates get a flat 8-column table. Columns: Client Name / Client Email / Trading Account # (payment.mt5Login or "—") / Product (challengeType + accountSize) / Date (locale) / Purchase Amount (currency-formatted) / Commission % / Commission Amount (in row's OWN currency via inline Intl formatter — commission currency can differ from brand currency, e.g. JPY payment → USD commission). Single useGetMyCommissions React Query hook keyed on `activePurchaseTier = Number(purchaseReportTab.replace("tier-",""))` — derived state keeps hook order stable across MLM/Standard variants (no conditional hooks). `PurchaseReportTable` declared at MODULE scope above AffiliateContainer to dodge the render-time component-inside-component remount footgun. Export CSV button → `apiClient.get(/affiliates/my-commissions?page=1&limit=0[&tier=N])` → 9-column CSV via file-saver with UTF-8 BOM (8 visible + Commission Currency as the 9th); filename `purchase-report[-tier-N]-YYYY-MM-DD.csv`. 13 new translation keys added to `messages/en.json` under `dashboard.affiliates.*` (purchaseReport, purchaseReportDescription, noPurchaseReportData, purchaseReportEmptyDescription, purchaseReportExportFailed, clientName, clientEmail, tradingAccount, product, purchaseAmount, commissionPct, commissionAmount, date) + 1 key to root `common.exportCsv` (used by `tc("exportCsv")`); `ja.json` deliberately NOT updated (falls back to key string — JA owned by separate `feat/email-qa-and-ja-localization` branch, matches 02-02/03-03/03-04 convention). Deviations: (1) `theme.classes.buttonSecondary` doesn't exist → used `buttonOutlinePrimary`; (2) next-intl `Translator` invariant generics rejected the prop signature → relaxed prop typing to `t: any` with eslint-disable comment; (3) `common.exportCsv` missing → added to ROOT common (not dashboard.common) because `tc` is wired to root; (4) dropped the `setPurchaseReportPage` setter (no pagination UI in scope — Referrals card is also unpaginated). All inline-fixes, no architectural changes. tsc clean on touched files (project tsc reports 259 pre-existing errors in `.next/types`, `scripts/migration/*`, `CheckoutEditorContainer` — none in our 4 files). 2 atomic commits pft-dashboard main-2026: fc6e4361 (endpoint config + hook + type) + 35337a41 (UI card + CSV export + translation keys); PUSHED to origin (6566f16d..35337a41); NOT deployed. Task 3 human-verify DEFERRED per objective (no dev server start). Verifies post-deploy on TradingCult (22 commission rows confirmed live across tiers 1/2/3): card renders below Your Referrals, MLM tabs filter via tier query param refetch, 9-column CSV opens cleanly in Excel, NOT visible on Withdrawals page.

04-03: Admin Payment History CSV export now ships affiliate commission columns (pft-dashboard main-2026). `ENDPOINTS.admin.affiliate.bulkCommissionsByOrders` added next to existing `commissionsByOrder` per-order lookup, pointing at Plan 04-01's `POST /affiliates/admin/commissions/bulk-by-orders`. `useExportPaymentsCsv` inserts ONE bulk POST `{orderIds: payments.map(p._id)}` (NOT N+1 per-row GETs — would also blow URL-length limit) between the lean payment fetch (skipEnrichment=true UNCHANGED) and the row-map; merges results client-side. CSV grew 19→25 columns: + Commission Rate (%) / Amount (USD) / Currency / Affiliate User ID / Affiliate Name / Affiliate Email at the tail (original 19 untouched). For MLM payments with multi-tier entries, lowest tier (tier 1 = direct referrer) wins per row via `.sort((a,b)=>(a.tier??1)-(b.tier??1))[0]`. Commission fetch wrapped in try/catch → `commByOrder={}` on failure, affiliate columns render empty but CSV still downloads (graceful degrade, no broken export). 2 atomic commits pft-dashboard main-2026: 97783483 (endpoint wiring) + 6566f16d (hook columns); PUSHED to origin; NOT deployed. No deviations. Verifies post-deploy: Network tab shows exactly 1 bulk-by-orders POST per export; CSV has 25 cols; payments with no commission show empty strings not "undefined".

04-01: Affiliate reporting backend endpoints (pft-backend main-2026). Two new routes for the Phase 4 affiliate-reporting UI work. `POST /affiliates/admin/commissions/bulk-by-orders` (Auth admin/backOffice/sales) — one `AffiliateCommission.find({orderId:{$in}})` query grouped into `Record<orderId, entry[]>` for the admin Payment History CSV export (unblocks Plan 03). `GET /affiliates/my-commissions` (Auth `userRole.user` ONLY — security-critical, no admin/backOffice/sales so the service can never be silently changed to accept an override userId) — paginated commission rows scoped to `req.user._id`, batched Payment join via `Promise.all` (not sequential await in .map, matches getAdminUserCommissions perf), surfaces `payment.mt5Login` when set; unblocks Plan 04 Purchase Report UI. Service funcs: `getCommissionsBulkByOrders(orderIds)` + `getMyCommissions(userId, {page,limit,tier,sortBy,sortOrder})` inserted between `getCommissionsByOrderId` and `selectLevelIndexByThreshold` in affiliate.service.ts; controllers + exports in `AffiliateController`. No deviations — plan was surgical and executed exactly as written. Pre-existing tsc errors (esModuleInterop config, Request.user typing across all controllers) NOT touched — they affect every controller, not the new code. Commits: pft-backend e136636c (service) + 63f7d44a (controller+routes), PUSHED to origin/main-2026; NOT deployed. Routes go live when main-2026 deploys.

03-04: Competition close + winner determination (COMP-05 + COMP-06). pft-backend (main-2026 2e914996): closeAndDetermineWinners(id) = THE single winner-write path — CAS gate findOneAndUpdate({_id,status:"active"},{status:"closing"},{new:true}); null => no-op return (already claimed / not active) — the ONLY guard against double winner determination. Ranks by final delta = current valueGrowthPercentage (Leaderboard collection) − baselineValueGrowth. Disqualifies THREE ban sources: Leaderboard.status in {BANNED,VIOLATED} + programs[].isBanned + top-level User.isBanned. Dedupe-by-user Map (best delta per user) => top N = N distinct users. Persists finalValueGrowth+delta on ALL entries (bulkWrite) for standings; marks winning entries rank+isWinner; writes winners[] snapshot; status->ended. Prize disbursement MANUAL (record-only, no payout/MT5). determineWinners(id) admin entry: ended/closing returns existing winners (idempotent, no recompute), active routes through CAS, else 400. getAdminResults(id): full-identity winners + full standings (populate firstName/lastName/email, sort delta desc) — NOT the public masked DTO. closeIfDue() cron hook now delegates to closeAndDetermineWinners (replaces 03-02 placeholder). Routes POST /:id/determine-winners + GET /admin/:id/results (Auth admin/backOffice). pft-dashboard (main-2026 1d1ececc): config determineWinners+adminResults endpoints; CompetitionAdmin{Winner,Standing,Results} full-name types; useDetermineWinners mutation + useCompetitionResults query; CompetitionResults.tsx (winners podium mirror of WeeklyPrizeWinners + full standings table, full names, in Dialog); CompetitionsTable "Determine winners" btn (past-end active, CAS-safe) + "Results" btn (ended); CompetitionContainer results modal + handler. Both PUSHED; NOT deployed. human-verify checklist in 03-04-SUMMARY.md (deferred — app not deployed).

03-03: Public competition surface (COMP-04). pft-backend (main-2026 a27eb855): competition.service listPublic() (active/ended only, public-safe projection) + getPublicRankings() (delta = current valueGrowthPercentage from Leaderboard − entry.baselineValueGrowth, sorted desc, masked via toPublicRankingDTO cloned from leaderboard.service toPublicDTO — displayName "John D.", NEVER email/lastName) + opt-out re-filter (User.distinct $nin at query time so opt-out AFTER enrollment vanishes); controller publicList/publicRankings with optional Bearer decode (anon never throws, valid token => includeRicherStats only, never identity); routes public GET "/" cacheResponse(30) + GET "/:id/rankings" cacheResponse(15, scope:user, keyExtra auth/anon) — MANDATORY bucket (route has no Auth). pft-dashboard (main-2026 133edad3): middleware isCompetitionsPath allowlist (mirror isLeaderboardPath); config publicList/publicRankings endpoints; usePublicCompetition hook (no auth branching, staleTime matches backend TTL); app/competitions/ list + [id] detail OUTSIDE (dashboard); PublicCompetitionContainer (prize pool + countdown + rankings, richer cols display-only) + CompetitionCountdown (date-fns + setInterval, cleaned on unmount) + PublicCompetitionRankingsTable (masked, slim, grep-clean). Deviation: theme.classes.bgWhite doesn't exist (under theme.colors) → used bgGray50. No separate public detail endpoint — detail page filters the public list by id. Stale/empty Leaderboard (non-prod, MT5_CRONS_ENABLED off) → current falls back to baseline → delta 0 (graceful). Both PUSHED; NOT deployed. human-verify checklist in 03-03-SUMMARY.md (deferred — app not deployed).

03-02: Activation auto-enroll + baseline snapshot + competition cron (pft-backend main-2026). enrollParticipants() writes ONE CompetitionEntry per funded (Program.programStage==="funded") non-opted-out account, baselineValueGrowth snapshot = Leaderboard.performance.valueGrowthPercentage; reuses leaderboard.service funded+opt-out query (no raw-User derivation); idempotent (skip if entries exist) + activation-only (no rolling enrollment). activate() now enrolls after draft→active flip. tickTransitions() = thin date dispatcher: due drafts → activate (enroll), due-active → closeIfDue() placeholder (03-04 fills CAS close + winners). CompetitionCronService (static class, isRunning guard, 5-min setInterval) registered server.ts:398 AFTER leaderboard cron block, NOT gated on MT5_CRONS_ENABLED. CAVEAT: cron not MT5-gated but the Leaderboard data it reads IS (leaderboard refresh gated MT5_CRONS_ENABLED) → enrollment/rankings stale/empty in non-prod. Deviation: User imported as default from ../Auth/auth.model (../User/user.model empty). Commit pft-backend 8dec4e3a, PUSHED; not deployed. 03-04 replaces closeIfDue() body.

03-01: Competition foundation across 3 repos. pft-backend (main-2026): Competition model (draft|active|closing|ended|archived state machine, prizePool[], winners[] subdoc, locked valueGrowthPercentage metric, NO per-brand field) + SEPARATE CompetitionEntry collection + zod validation + admin CRUD (service/controller/routes) gated Auth(admin,backOffice), draft-only edit/delete guards, activate(draft→active)/deactivate(active→draft, blocked once entries exist), registered /competitions; admin reads under /competitions/admin (bare / + /:id/rankings reserved for 03-03). pft-dashboard (main-2026): competition.types + competitions ENDPOINTS + useCompetitions hooks + /admin/competitions page (table + Coupon-pattern modal, Edit/Delete disabled non-draft, Enable/Disable toggle) + sidebar entry. pfr-super-admin (main): /admin/competitions pagePermissions seed. Commits: pft-backend 2d7b8949 + 2a360a99, pft-dashboard 9e63857b, pfr-super-admin 69d3669. All PUSHED. activate() is the 03-02 enrollment hook point.

02-04: Public leaderboard filters + sort complete — new PublicLeaderboardFilters (account-size + challenge-type selects sourced from data.filters.available*, sort-by % growth / win rate / profit factor + asc/desc toggle) lifted into PublicLeaderboardContainer state → usePublicLeaderboard params (page resets to 1 on change). LB-04 vs 02-01 contradiction RESOLVED: un-stripped challengeType in getPublicLeaderboard controller (option a) — safe because service forces programStage="funded" AFTER spreading caller filters, so challengeType only narrows within funded. Funded-only/masking/PII guarantees untouched; grep clean. Committed pft-dashboard main-2026 (74172f4b hook+types, 9f1cf5a0 filters+container) + pft-backend main-2026 (7e0acf28 controller); not deployed. human-verify checklist in 02-04-SUMMARY.md.

02-02: Public leaderboard UI complete — /leaderboard page outside (dashboard) auth group, middleware-whitelisted; usePublicLeaderboard hook + slim PublicLeaderboardContainer/Table consuming GET /leaderboard/public; masked displayName only, richer columns (Account Size, Trading Days) conditional on logged-in fields; no PII/admin routing (grep clean). Committed pft-dashboard main-2026 (def211e8, f75af977); not deployed. Filter/sort slot reserved for 02-04.

02-01: Public endpoint GET /leaderboard/public complete — masked DTO (no PII), funded-only + opt-out filters, optional-token richer stats, auth/anon cache buckets. Committed pft-backend main-2026 (9399ecc9, 586949ca); not deployed.

## Performance Metrics

**Velocity:**
- Total plans completed: 5 (Phase 2: 02-01–02-04; Phase 3: 03-01)
- Average duration: ~5 min
- Total execution time: <1 hour

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| - | - | - | - |

**Recent Trend:**
- Last 5 plans: —
- Trend: —

*Updated after each plan completion*

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Architecture: toPublicDTO() serializer built in same phase as LB-01 (Phase 2) — prevents PII leak
- Architecture: CompetitionEntry as separate collection (not embedded) — avoids 16MB BSON limit at 10k+ participants
- Architecture (Phase 3 REVERSAL): OMIT brandId on Competition — zero existing models carry it; multi-brand is per-DB separation. Supersedes earlier "brandId from day one" note.
- Phase 3 decisions: disqualify BANNED/VIOLATED at close; per-account entry deduped to best account per user at win (top 3 = distinct users); rank by valueGrowthPercentage delta from activation baseline
- Architecture: CAS close pattern for competition close — prevents double winner determination
- Architecture: Baseline snapshot per participant at competition start — rank by delta, not absolute value
- 01-01: floatingPL MT5-offline fallback is deterministic 0 (not random) — keeps leaderboard ranks stable across requests
- 01-02: leaderboardOptOut added via Mongoose `default: false` — no migration; `{ leaderboardOptOut: false }` is now a safe filter
- Repo split: backend code commits land in nested `pft-backend` repo on `main-2026`; `.planning/` docs live in parent repo
- 02-03: dashboard `User` type lives in `src/types/user.types.ts` (not `index.ts`); `leaderboardOptOut` added there + to `useUpdateUser` payload allow-list
- 02-03: opt-out is frontend-only — `PATCH /users/:id` already self-updates `leaderboardOptOut` (raw findByIdAndUpdate, not in sensitive-strip)
- 02-01: public masking is UNIVERSAL (anon + logged-in get "John D."); valid Bearer token unlocks only richer STAT fields, never identity
- 02-01: public cache MUST bucket auth vs anon via `keyExtra` (route has no Auth, so `req.user` undefined → scope:user alone collapses all to "anon" and leaks richer stats)
- 02-01: funded-only forced in service; opt-out applied query-time via `User.distinct` + `userId $nin`, merged field-wise with search `$in` through new `extraMatch` param on `getLeaderboard`
- 02-02: public UI types added to existing `src/types/leaderboard.types.ts` (re-exported via `@/types`), not a new `leaderboard.ts`
- 02-02: logged-in vs anon is DISPLAY-only — richer columns rendered from presence of richer stat fields in the response (apiClient auto-attaches token); never a branched fetch
- 02-02: dedicated slim public components built (admin `LeaderboardTable` not reusable — renders email + pushes /admin/users); public surface is grep-clean of PII/admin routing
- 02-02: `/leaderboard` whitelisted in middleware via `isLeaderboardPath` OR-clause; page lives outside `(dashboard)` auth group
- 02-04: LB-04 challengeType filter IS supported — resolved the 02-01 strip vs LB-04 contradiction by un-stripping challengeType in the public controller (option a). Safe because `getPublicLeaderboard` service spreads caller filters then sets `programStage: "funded"` AFTER → challengeType only narrows within funded, never widens. masking/toPublicDTO path untouched
- 02-04: public filter/sort is container-owned state feeding usePublicLeaderboard (single source of truth); PublicLeaderboardFilters is presentational. Radix Select "All" uses a `__all__` sentinel (empty-string item values disallowed). Filter/sort change resets page→1
- 02-04: challengeType dropdown options come from `Program.distinct("challengeType")` (all programs) — a listed type with no funded traders yields an empty table (cosmetic, no leak); could later narrow to funded-present types
- 03-03: public competition rankings reuse the EXACT Phase 2 security trio — toPublicRankingDTO masking (clone of leaderboard toPublicDTO, displayName only), opt-out re-filter (User.distinct $nin at query time), and the auth/anon cacheResponse keyExtra bucket on the no-Auth `/:id/rankings` route. All three are mandatory and grep-verified clean of email/lastName
- 03-03: live competition rank = current valueGrowthPercentage (read from the Leaderboard collection, keyed userId+programId) − entry.baselineValueGrowth (the activation snapshot). LOCKED delta, not absolute. No live Leaderboard row → current falls back to baseline → delta 0
- 03-03: no separate PUBLIC competition DETAIL endpoint — the `/competitions/[id]` page sources meta from the public LIST endpoint (filter by id) and rankings from `/:id/rankings`. Public list + rankings are both gated to status in [active, ended] (never draft)
- 03-03: `theme.classes.bgWhite` does NOT exist (it lives under `theme.colors`); use `theme.classes.bgGray50` for card backgrounds
- 03-03: `/competitions` pages live OUTSIDE `(dashboard)` (sibling of `/leaderboard`), whitelisted via `isCompetitionsPath` in middleware; public competition types added to existing `competition.types.ts` (imported directly via `@/types/competition.types` — competition.types is NOT re-exported from `@/types/index`)
- 03-04: the CAS active->closing flip (`findOneAndUpdate({_id,status:"active"},{status:"closing"},{new:true})`) is the ONLY guard against double winner determination; cron `closeIfDue` + admin `POST /:id/determine-winners` both call `closeAndDetermineWinners` so there is exactly ONE winner-write path. A null CAS result (already claimed / not active) is a safe no-op
- 03-04: winner disqualification checks THREE ban sources — Leaderboard.status in {BANNED,VIOLATED}, programs[].isBanned, AND top-level User.isBanned (strict superset of the plan's named sources)
- 03-04: `finalValueGrowth` + `delta` are persisted on ALL CompetitionEntry rows at close (not just winners) so the admin standings table renders the full board; dedupe-by-user keeps each user's highest-delta eligible entry → top N = N distinct users
- 03-04: admin results read their OWN full-identity data (firstName/lastName/email via populate) through `GET /admin/:id/results` + `getAdminResults` — NEVER the public masked `toPublicRankingDTO`. `determineWinners` on an already-ended/closing competition returns existing winners WITHOUT recompute (idempotent)
- 04-01: `/affiliates/my-commissions` is locked to `Auth(userRole.user)` ONLY. Admin/backOffice/sales deliberately excluded — service derives userId from `req.user._id`, so adding admin roles would either show admins only their own commissions (confusing UX) or, worse, set up a silent privilege-bypass regression if the service is later refactored to accept an override userId. Route file carries a comment documenting this so it doesn't get "fixed" later
- 04-01: bulk-by-IDs lookup pattern — `POST` with `{ orderIds: string[] }` body, returns `Record<orderId, entry[]>` from a single `$in` query; caller pivots client-side. Avoids N round-trips when admin exports hundreds of payments
- 04-01: paginated-with-Payment-join pattern uses `Promise.all` over `Payment.findById` calls (NOT sequential await in `.map`) — matches `getAdminUserCommissions` perf; sequential await across page-size rows is the regression to guard against in code review

### Pending Todos

- Phase 5 (ad-hoc) added 2026-06-30 — Daily Profit Display Bug for Trading Cult account 13535 (ticket cmquy9bqo005pny0kw6j0lr71, URGENT). Dashboard Daily P&L Calendar shows +$55 for 2026-06-18; MT5 Daily Reports + History both confirm $9.90. Bug-debug, not feature. Next: `/gsd:plan-phase 5`. Likely candidates: tradehistories daily aggregation, dailysnapshots timezone bucketing, swap/commission inclusion, hedge-leg double-count.
- **Phase 4.1 COMPLETE (2026-06-30)** — Affiliate Reporting Bug Fixes shipped. Bug 1 (CSV tier-sum + Direct Commission Rate header rename) landed as pft-dashboard main-2026 `60e9b37c`; Bugs 2 + 3 closed pre-execution by remote hotfixes `7110ce54 / bc0ef4ec / 31ab6d1b` (USD-canonical Purchase Report + `formatAccountSizeLabel`). v1.1 audit fully closed. Human-verify DEFERRED to post-deploy: screenshot Purchase Report to ticket `cmqqchwh500bspi0kxw23o2rl` → WAITING_CLIENT.
- **TODO 2026-06-30 RESOLVED** — Payment History customer-link regression (ticket cmquhk6f9005dny0kgrlbemsy). Root cause: my fix `4a345ff3` trusted `Payment.userId: string` type but TC backend delivers a populated user object at runtime → `/admin/users/[object Object]` → "undefined undefined's Profile". FIX (pft-dashboard main-2026 `f9da250e`, PUSHED): added `resolveUserIdStr(userId)` module-level helper (typeof string ? userId : userId?._id ? String(.) : "") + routed all 3 `payment.userId`-built URLs through it (customer-name `<Link>`, fraud-check button onClick, user-LTV button onClick). 1 file, +43/-20. Type left as `string` for now; broader follow-up to fix the type + audit all other consumers (10+ call-sites flagged earlier). Ticket reply posted + WAITING_CLIENT. Todo file moved to `.planning/todos/done/`. PATTERN NOTE: same class as Phase 4 bug 3 (`Number("5k")` NaN) — TS type lies, no pft-dashboard runtime tests catch it.

### Roadmap Evolution

- 2026-06-30: Phase 5 added (ad-hoc, post-v1.1) — Daily Profit Display Bug — Account 13535 (ticket cmquy9bqo005pny0kw6j0lr71). Sits outside both v1.0 and v1.1 milestones; will be bundled into the next milestone or remain ad-hoc depending on scope.
- 2026-06-30: Phase 6 added (ad-hoc, post-v1.1) — Funded Queue Ready Badge (ticket cmqt9rtjl002rny0kkawu1c6y, Trading Cult HIGH). Sidebar red-dot when KYC+contract both approved on pending funded queue entries.
- 2026-06-30: Phase 7 added (ad-hoc, post-v1.1) — Used Margin Display client + backoffice (ticket cmovizb320007qs0k0fue250p, Trading Cult BACKLOG). Risk-monitoring metric — current + peak margin %.
- 2026-06-30: Phase 8 added (ad-hoc, post-v1.1) — Breach Email Template Vars (PRIMARY ticket cmr0ufshl00obny0kz3zk1uju XPIPS LOW + RELATED ticket cmqv8nmco006fny0kdrvlcugw Funding Optimal HIGH — same root cause from different angles: XPIPS admin sick of replying with breach reason; FO clients confused why account breached when balance > floor. Tick-based EQUITY check fires when balance+floatingPnL dips below floor, but post-breach positions close and balance lands ABOVE floor → user sees impossible-looking state. Surface reason in email closes the loop). Data already passed (verified live XPIPS DB — `args.message`/`ban_reason`/`violation_details` carry exact "Tick-based breach: ..." string + `args.breach_date` + 30+ fields); fix is template-side only (extend `messagetemplates.variables` registry + add `{{ban_reason}}` interpolation on 4 breach templates: Rule Breached Template / Funded Account Breach / Leverage Exceeded Breach / inactivity breached email). NO rule-checker code change. Per-brand mongosh sync script for existing customised templates. FO deeper UX asks (a) balance==equity at breach moment, (b) force balance display < floor — explicitly OUT OF SCOPE; ship Phase 8 first + measure complaint volume before scoping any Phase 9 UX overhaul.
- 2026-06-30: **Phase 4.1 INSERTED (URGENT)** — Affiliate Reporting Bug Fixes. Triggered by client reopen of source ticket cmqqchwh500bspi0kxw23o2rl ~1h after v1.1 archive+tag. Retroactive audit `.planning/v1.1-MILESTONE-AUDIT.md` confirms 3 bugs in Phase 4 deliverables: (1) CSV tier-1-only should be SUM across tiers, (2) Purchase Report Commission Amount uses brand currency code instead of row currency (¥1 instead of $1.25), (3) Product label `twoStep $NaN` from `Number("5k")`. v1.1 archive remains accurate as snapshot; 4.1 is the patch — does NOT reopen v1.1.

### Blockers/Concerns

- PRE-01/PRE-02 must land before Phase 2 work — do not skip
- leaderboardOptOut schema default landed (01-02) — `{ leaderboardOptOut: false }` is now safe; schema must deploy from `main-2026` before leaderboard service relies on it at runtime
- 02-01 public endpoint committed on `pft-backend` main-2026 but NOT deployed; it reads `leaderboardOptOut` at runtime, so 01-02 schema + 02-01 code must both ship from main-2026 before the endpoint goes live
- 03-03 public competition endpoints committed on `pft-backend` main-2026 (a27eb855) but NOT deployed; they read `leaderboardOptOut` + the Leaderboard collection at runtime — 01-02 schema + 03-02 enrollment + 03-03 endpoints must all ship from main-2026 before the public competition surface goes live
- 03-03 rankings depend on a fresh Leaderboard collection for the live `current` value; in non-prod where `MT5_CRONS_ENABLED` is off the collection is stale/empty, so deltas read 0 (expected, not a bug)
- 03-04 winner determination committed on `pft-backend` main-2026 (2e914996) but NOT deployed; the CAS close + winner snapshot only runs once the competition module ships from main-2026. It reads the Leaderboard collection for the final `current` value — in non-prod where `MT5_CRONS_ENABLED` is off, deltas read 0 / no eligible winners (expected Pitfall 2; CAS + disqualify + distinct-user logic still verifiable wherever live data exists, idempotency verifiable regardless)

## Session Continuity

Last session: 2026-06-29
Stopped at: Phase 3 plan 03-04 (CAS close + winner determination — COMP-05 + COMP-06) COMPLETE — this is the FINAL plan of the competition phase. 2 auto tasks committed AND pushed (pft-backend main-2026 2e914996, pft-dashboard main-2026 1d1ececc). Task 3 was a human-verify checkpoint, intentionally DEFERRED (app not deployed) — checklist recorded in 03-04-SUMMARY.md. Phase 3 (and all roadmap phases) now complete pending deploy + live verification.
Resume file: no further plans queued. Next action = deploy main-2026 (all three: pft-backend, pft-dashboard, rule-checker) and run the pending human-verify checklists below against live data.
Pending human-verify checklists: 02-02-SUMMARY.md + 02-03-SUMMARY.md + 02-04-SUMMARY.md + 03-03-SUMMARY.md + 03-04-SUMMARY.md (all await live deploy).
