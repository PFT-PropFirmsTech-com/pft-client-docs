# Requirements Archive: v1.3 CRM Partner Tracking (S2S Postbacks)

**Archived:** 2026-07-01
**Status:** ✅ SHIPPED (code-complete + code-level verified; live human-verify deferred pending deploy + partner config)

For current requirements, see `.planning/REQUIREMENTS.md` (fresh one created by the next `/gsd:new-milestone`).

---

# Requirements: PFT WhiteLabel v1.3 — CRM Partner Tracking (S2S Postbacks)

**Defined:** 2026-07-01
**Core Value:** A Trading Cult affiliate partner can attribute registrations and conversions to their own traffic by passing a `clickid` through our funnel unchanged and receiving S2S postbacks (with clickid + goal + payout) on registration and first sale.

**Source ticket:** cmqt52jdb001dny0kknkou9x0 (Trading Cult Pro)
**Research:** `.planning/research/SUMMARY.md` (+ STACK/FEATURES/ARCHITECTURE/PITFALLS)

## Scope decisions (locked 2026-07-01)

- **S2S postbacks only** — pull API (partner alternative B) deferred.
- **Minimal one-off for the Trading Cult partner** — config-driven per-brand, NOT a generic multi-partner admin UI.
- **Conversion = FTD** — fire the conversion postback once, on the user's first completed purchase (across standard challenge AND PAP funded-leg purchases).
- **Payout value = `usdAmount`** (normalized USD, `currency=USD` always) — guards the known JPY-billed-as-USD bug class.
- **PAP counts as a conversion** — the currently-unwired `pap_payment_completed` path must emit so PAP first-purchases fire.
- Free-trial / free-challenge ($0) = **registration postback only**, never a $0 conversion postback (S2S fraud-filter risk).

## v1.3 Requirements

### Click ID Capture & Persistence

- [x] **CRM-01**: A partner tracking-link entry (`GET /track?clickid=…`, brand landing domain) captures the arbitrary partner `clickid`, sets it in a first-party cookie, and redirects to the site so the clickid survives to the registration step. — **Shipped Phase 10 (10-01, backend `09ca7387` + dashboard `e111dab1`). Real endpoint: `GET /api/tracking/track?clickid=…` (mounted under the existing tracking router).**
- [x] **CRM-02**: The partner `clickid` is persisted **unchanged** on the User document at registration (`verifyRegistrationOtp`), so it survives from click → registration. — **Shipped Phase 10 (10-02, backend `d2992553`). Survives via the existing registration payload spread — no `auth.service.ts` edit needed.**
- [x] **CRM-03**: The partner `clickid` is persisted on the Payment document at checkout creation, so a later purchase completed via a gateway/webhook callback (where `req = null`, no cookie) can still resolve the clickid. — **Shipped Phase 10 (10-03, backend `4a079169`). Persisted at both standard checkout AND PAP funded-leg creation, from the authoritative `user.partnerClickId`.**

### Registration Postback

- [x] **CRM-04**: When a user completes registration, an S2S GET postback fires to the partner's registration URL template with `goal=registration` + the user's `clickid` (no payout). Fires once per user; skipped when the user has no partner `clickid`. — **Shipped Phase 11 (11-01, backend `8e2f7509`+`44deb3d4`) wires the event; Phase 12 (12-02) sends it.**

### Conversion Postback

- [x] **CRM-05**: On a user's **first** completed purchase (FTD — first across standard challenge purchases AND PAP funded-leg purchases), an S2S GET conversion postback fires with `goal=conversion` + `clickid` + `payout=<usdAmount>` + `currency=USD`. Later purchases by the same user do NOT fire again. — **Shipped Phases 11+12. Design refinement: `purchase_completed`/`pap_payment_completed` fire on EVERY purchase (Meta/GA4/Klaviyo need every one) carrying an `isFirstPurchase` flag; the once-per-user CONVERSION POSTBACK gate lives in the Phase 12 adapter (12-02), not in event suppression.**
- [x] **CRM-06**: The PAP funded-leg payment-completion path (currently not wired to any conversion webhook) emits the purchase event carrying the partner `clickid` + `usdAmount`, so PAP first-purchases are eligible for the conversion postback. — **Shipped Phase 11 (11-02, backend `644ccd39`+`982ba9a1`). Also fixed a pre-existing JPY-as-USD bug: `papPaymentCompleted` was passing billed `payAfterPassRemainingPrice`, switched to `usdAmount` at all 3 sites (callback/stripe/fanbasis).**

### Postback Delivery

- [x] **CRM-07**: A new `partnerPostback` destination adapter sends a GET request built by substituting `{clickid}` / `goal` / `{payout}` macros into the configured URL template (URL-encoded), fire-and-forget with a timeout, bounded retry/backoff on partner 5xx, and a delivery-log record — reusing native `fetch` (no new deps) and the existing `IDestinationAdapter` contract. — **Shipped Phase 12 (12-02, backend `719e591b`). 10/10 behavioral harness pass. Dedup + delivery-log inherited free from the existing dispatcher — the adapter only implements `send()`.**
- [x] **CRM-08**: Each postback event (registration, conversion) fires **at most once** per user even under retries / gateway webhook re-delivery — deduped via the existing `TrackingEventLog`, and the legacy dual-dispatch path is audited so it cannot double-fire the same event. — **Shipped Phase 11 (11-03, `8540f5a`). Stable eventIds (`signup:<userId>`, `purchase:<paymentId>`, `pap:<paymentId>`) make retries idempotent. Legacy `ConversionWebhookEventsService` confirmed disjoint (KYC/payout/challenge only, 6 methods) — no double-fire risk.**

### Configuration

- [x] **CRM-09**: The partner's registration + conversion postback URL templates live in the per-brand `TrackingSettings.destinations.partnerPostback` config (Trading Cult DB only), with an enable/disable toggle; nothing partner-specific is hardcoded, and no other brand fires postbacks unless configured. — **Shipped Phase 12 (12-01, backend `702b312f`). Disabled + empty by default. Zod PUT-validation block added (zod strips unknown keys — without this the config would silently fail to save).**

## v1.4 / Future Requirements (carried forward)

- **CRM-10**: Refund / chargeback reversal postback — fire a `goal=refund` (or negative-payout) postback when a first-purchase is later refunded/charged back (no reversal path exists today; partner commissions currently stand on refunds).
- **CRM-11**: Pull API (partner alternative B) — partner-authenticated endpoint to retrieve clickid / event / status / payout.
- **CRM-12**: Generic multi-partner postback config (admin UI, per-partner templates + goal mappings) — build when a 2nd partner appears.

## Out of Scope (v1.3)

| Feature | Reason |
|---------|--------|
| Pull API | Postbacks satisfy the partner now; API is their alternative, not both (→ CRM-11) |
| Generic multi-partner admin UI | Only one partner today; premature abstraction (→ CRM-12) |
| Refund/chargeback reversal postback | No reversal path exists today; scope + partner contract call (→ CRM-10) |
| Every-purchase conversions | FTD chosen — one conversion per acquired user |
| Reusing the existing `conversionWebhook` adapter | Wrong event map + wrong wire protocol (POST+JSON+HMAC vs GET+macro) — new adapter instead |

## External dependency (non-blocking — config-driven, now the live blocker)

Partner's exact postback spec (macro param names beyond `clickid`/`goal`/`payout`, GET confirmation, whether they want an order/transaction id). The adapter is template-driven — Trading Cult's real `registrationUrl`/`conversionUrl` values are set post-deploy via `PUT /api/tracking/settings`, no code change needed. This is now the single open item before CRM-01 through CRM-09 can be live-verified end to end (Phase 10's 10-04 checkpoint + Phase 12's SC5 checkpoint).

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| CRM-01 | Phase 10 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-02 | Phase 10 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-03 | Phase 10 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-04 | Phase 11 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-05 | Phase 11 + 12 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-06 | Phase 11 | ✅ Complete (code-verified; live-verify deferred) |
| CRM-07 | Phase 12 | ✅ Complete (code + 10/10 behaviorally verified; live-verify deferred) |
| CRM-08 | Phase 11 | ✅ Complete (audited, confirmed disjoint) |
| CRM-09 | Phase 12 | ✅ Complete (code-verified; live-verify deferred) |

**Coverage:** 9 of 9 requirements shipped.

---

## Milestone Summary

**Shipped:** 9 of 9 requirements (100%).

**Adjusted:** CRM-05's mechanism refined during Phase 11 planning — originally implied event suppression on repeat purchases; corrected to an `isFirstPurchase` flag (events still fire for shared destinations Meta/GA4/Klaviyo) with the once-per-user gate enforced in the Phase 12 adapter instead. Same requirement outcome, more correct implementation.

**Dropped:** none.

**Bonus fix (not a formal requirement, discovered mid-milestone):** `pap_payment_completed`'s payout field was passing the billed `payAfterPassRemainingPrice` (could be JPY) instead of normalized `usdAmount` — this is exactly the bug class from `project_pap_jpy_charged_as_usd.md`. Fixed at all 3 PAP payment-completion sites (callback/stripe/fanbasis) as part of CRM-06.

---
*Archived: 2026-07-01 as part of v1.3 milestone completion*
