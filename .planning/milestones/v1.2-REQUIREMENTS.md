# Requirements Archive: v1.2 Ticket Fixes + PAP Queue Label

**Archived:** 2026-07-01
**Status:** ✅ SHIPPED (code-complete + pushed; human-verify deferred pending deploy)

For current requirements, see `.planning/REQUIREMENTS.md` (recreated fresh by the next `/gsd:new-milestone`).

---

# Requirements: PFT WhiteLabel v1.2 — PAP Funded Queue State Label

**Defined:** 2026-07-01
**Core Value:** Support sees the actual PAP funded-queue state instead of a generic "Program Not Assigned" warning, so a compliance gate stops being mistaken for a technical failure.

## v1.2 Requirements

### PAP Funded Queue UX

- [x] **PAP-01**: Admin payments view shows the real `fundedprogressionqueues` state for PAP funded-leg rows — `Awaiting KYC` / `Awaiting Contract` / `In Funded Queue` — when a queue entry exists in `pending`/`processing`. The generic "Program Not Assigned" label + Retry/Mark Done buttons render only when there is genuinely no queue entry. — **Shipped Phase 9 (pft-backend `5de7c9f8`, pft-dashboard `5dea14f2`); 9/9 code must-haves verified; live human-verify deferred pending deploy.**

## v1.3 / Future Requirements (carried forward)

- **PAP-02**: Retry button suppress/relabel for PAP funded legs (hide payment-level retry or repoint at queue-level `/funded-queue/:id/retry`). Depends on PAP-01 label taxonomy (now locked).
- **PAP-03**: Refresh queue `reason` field alongside flag sync in `FundedProgressionQueueService.processQueueForUser` so `reason: "both_pending"` doesn't linger after `contractApproved` flips true.
- Winner email notifications on competition close (from v1.0).
- Competition history / hall of fame view (from v1.0).
- Automated prize disbursement — MT5 credit + payout (from v1.0).
- Broader admin-panel anchor refactor for open-in-new-tab / copy-link (DEV cmqztddis).

## Out of Scope (v1.2)

| Feature | Reason |
|---------|--------|
| Auto-approve KYC to bypass the gate | Compliance requirement — human review is correct |
| Client-facing status message on trader dashboard | Trader already sees KYC pending in the KYC widget; admin UX is the pain point |
| Multi-tier queue state (queue position number) | Cosmetic — no observed support pain, adds query overhead |
| Real-time push of queue state changes | 5-min cron + on-KYC-approve auto-release is sufficient |
| Rework of the queue `reason` enum values | Scope creep — PAP-03 is the smaller version |

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| PAP-01 | Phase 9 | ✅ Complete (code-verified; human-verify deferred) |

**Coverage:** 1 formal requirement, mapped + shipped.

---

## Milestone Summary

**Shipped:** 1 of 1 formal requirement (PAP-01).

**Swept-in ad-hoc work (no formal REQ-ID — ticket-driven):** Phases 4.1, 5, 6, 7, 8 were client/support ticket fixes grouped into v1.2 at completion time so nothing stayed orphaned. See `v1.2-ROADMAP.md` for full detail. Tickets: cmqqchwh500bspi0kxw23o2rl (affiliate CSV), cmquy9bqo005pny0kw6j0lr71 (daily P&L), cmqt9rtjl002rny0kkawu1c6y (funded-queue badge), cmovizb320007qs0k0fue250p (margin display), cmr0ufshl00obny0kz3zk1uju + cmqv8nmco006fny0kdrvlcugw (breach email vars).

**Adjusted:** none.
**Dropped:** none (Phase 4.1 Bugs 2+3 and Phase 6 were closed by remote hotfixes, not dropped — same audit outcome, different shape).

---
*Archived: 2026-07-01 as part of v1.2 milestone completion*
