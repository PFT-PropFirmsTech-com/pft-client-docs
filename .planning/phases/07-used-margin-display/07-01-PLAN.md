---
phase: 07-used-margin-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-rule-checker/src/app/models/accountRuleState.model.ts
  - pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts
autonomous: true

must_haves:
  truths:
    - "accountrulestates documents on pft-rule-checker now carry currentMarginUsedPercent and peakMarginUsedPercent fields"
    - "Every rule-state tick (the same site that already updates peakDailyDrawdownPercent / peakTotalDrawdownPercent) writes a fresh currentMarginUsedPercent = (margin / equity * 100) and ratchets peakMarginUsedPercent via Math.max"
    - "Peak is monotonically non-decreasing within an account's life — daily EOD reset paths do NOT zero peakMarginUsedPercent"
    - "Edge cases handled: equity <= 0 or margin missing -> currentMarginUsedPercent = 0 (no NaN/Infinity)"
    - "pft-backend reads the new fields through its existing strict:false AccountRuleState model with ZERO backend code change"
    - "No retroactive backfill — peak starts populating from rule-checker deploy date"
  artifacts:
    - path: "pft-rule-checker/src/app/models/accountRuleState.model.ts"
      provides: "Schema fields currentMarginUsedPercent + peakMarginUsedPercent (Number, default 0)"
      contains: "peakMarginUsedPercent"
    - path: "pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts"
      provides: "Math.max ratchet for peakMarginUsedPercent + fresh currentMarginUsedPercent computation, persisted on every tick alongside existing peak fields"
      contains: "peakMarginUsedPercent"
  key_links:
    - from: "ruleStateService.updateRuleStateRT peak block (~line 504-521)"
      to: "accountrulestates document via $set/save"
      via: "same write path as peakTotalDrawdownPercent"
      pattern: "peakMarginUsedPercent.*Math\\.max"
    - from: "eodService daily/payout reset paths"
      to: "accountrulestates daily-reset $set block"
      via: "EXCLUSION — peakMarginUsedPercent NOT in the reset $set"
      pattern: "NOT peakMarginUsedPercent"
---

<objective>
Extend pft-rule-checker's accountrulestates collection with two new fields — currentMarginUsedPercent (live snapshot) and peakMarginUsedPercent (all-time ratchet) — and populate them inside the existing per-tick peak-metric block in ruleStateService.ts. Mirrors the proven peakTotalDrawdownPercent / highestEquityEver pattern exactly. Zero pft-backend code change (existing strict:false passthrough model surfaces the fields automatically).

Purpose: Persistence backbone for Phase 7. Dashboard widget (07-02) reads peak from this collection; current is also persisted here for parity / debugging even though the UI reads live current from the socket.

Output: Two new schema fields + ~6 lines of math in ruleStateService.ts + verification that daily/EOD reset paths do NOT zero the peak.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-used-margin-display/07-RESEARCH.md

# Existing peak-metric pattern to mirror exactly
@pft-rule-checker/src/app/models/accountRuleState.model.ts
@pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts

# pft-backend strict:false passthrough model — confirms zero backend change needed
@pft-backend/src/app/models/accountRuleState.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add currentMarginUsedPercent + peakMarginUsedPercent to accountrulestates schema</name>
  <files>pft-rule-checker/src/app/models/accountRuleState.model.ts</files>
  <action>
Add two new schema fields next to the existing `peakTotalDrawdownPercent` field (around line 47):

```typescript
peakMarginUsedPercent: { type: Number, default: 0 },        // All-time peak of margin/equity*100 — monotonically non-decreasing
currentMarginUsedPercent: { type: Number, default: 0 },     // Latest snapshot of margin/equity*100 — overwritten every tick
```

Also add the same two fields to the TypeScript interface at `pft-rule-checker/src/app/models/accountRuleState.interface.ts` (mirror the existing peakTotalDrawdownPercent / highestEquityEver typing — both are `number`).

Do NOT change strict mode, indexes, or default values. Do NOT add to any existing index — these are read fields, not query keys.

Rationale: The pft-backend `accountRuleStateSchema` is declared `strict: false` (see pft-backend/src/app/models/accountRuleState.model.ts) — the new fields propagate to backend reads with zero backend code change. Verified in research §Pattern 3.
  </action>
  <verify>
1. `grep -n "peakMarginUsedPercent\|currentMarginUsedPercent" pft-rule-checker/src/app/models/accountRuleState.model.ts` shows both fields with `type: Number, default: 0`.
2. `grep -n "peakMarginUsedPercent\|currentMarginUsedPercent" pft-rule-checker/src/app/models/accountRuleState.interface.ts` shows both fields typed as `number`.
3. `cd pft-rule-checker && npx tsc --noEmit src/app/models/accountRuleState.model.ts` (or `npm run typecheck` if available) passes.
  </verify>
  <done>Schema + interface both carry the two new Number fields with default 0; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Compute + persist current/peak margin used % inside the existing tick block</name>
  <files>pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts</files>
  <action>
Locate the existing peak-metric block around lines 504-521 of `updateRuleStateRT` (the block that reads `previousPeakTotal = (state as any).peakTotalDrawdownPercent || 0` and computes `peakTotalDrawdownPercent = Math.max(previousPeakTotal, totalDrawdownPercent)`).

Immediately after that block, add:

```typescript
// All-time peak margin used % — same cadence as peakTotalDrawdownPercent, ratchet via Math.max
// Denominator decision (research §Open Question 2 + Pitfall 1): margin / equity * 100 (NOT MT5 marginLevel = equity/margin*100)
const previousPeakMargin = (state as any).peakMarginUsedPercent || 0;
const currentMarginUsedPercent =
  currentEquity > 0 && typeof currentMargin === "number" && currentMargin > 0
    ? (currentMargin / currentEquity) * 100
    : 0;
const peakMarginUsedPercent = Math.max(previousPeakMargin, currentMarginUsedPercent);
```

`currentMargin` must come from the same upstream source already feeding the peak block (the rule-state input carries `margin` from batch-snapshot / MT5 user-info polls). If the variable isn't already in scope at line ~504, pull it from the same source the function already uses to read `currentEquity` (check the function signature / destructure at the top of `updateRuleStateRT`). If margin is not in the input shape, add it to the destructured input — it's already on the upstream batch-snapshot cache per research §Architecture Patterns.

Then, in EVERY downstream `$set` / save block in this same function where `peakTotalDrawdownPercent` is written (find via `grep -n "peakTotalDrawdownPercent" ruleStateService.ts` — lines ~635, 658, 717), ALSO write `currentMarginUsedPercent` and `peakMarginUsedPercent` in the same shape:

```typescript
peakTotalDrawdownPercent,
peakMarginUsedPercent,        // NEW — all-time ratchet
currentMarginUsedPercent,     // NEW — fresh snapshot, overwrites every tick
```

CRITICAL — Daily / payout / EOD reset paths must NOT zero `peakMarginUsedPercent`. Audit these three sites and confirm:

1. `pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts` payout-reset block (~line 1075-1086, where `peakTotalDrawdownPercent: 0` IS reset) — explicitly leave `peakMarginUsedPercent` OUT of the reset $set. Add a one-line comment: `// peakMarginUsedPercent is all-time, NOT reset on payout/daily`.
2. `pft-rule-checker/src/app/services/rule-engine/eodService.ts` — `grep -n "peakDailyDrawdownPercent\|peakTotalDrawdownPercent" eodService.ts` to find any daily-reset $set blocks. peakMarginUsedPercent MUST NOT appear in any of them.
3. `pft-rule-checker/src/app/services/rule-engine/dailyResetScheduler.ts` — same audit. peakMarginUsedPercent MUST NOT be reset there.

Document the EOD-exclusion audit in the SUMMARY (one line per file you audited + result).

NO retroactive backfill. Peak starts populating from deploy date forward.
  </action>
  <verify>
1. `grep -n "peakMarginUsedPercent" pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts` shows: the Math.max ratchet block, plus inclusion in all downstream $set/save blocks where peakTotalDrawdownPercent appears.
2. `grep -n "peakMarginUsedPercent" pft-rule-checker/src/app/services/rule-engine/eodService.ts pft-rule-checker/src/app/services/rule-engine/dailyResetScheduler.ts` shows ZERO matches inside any reset $set block (matches only in pass-through reads if any).
3. `grep -n "currentMarginUsedPercent" pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts` shows the per-tick compute + inclusion in $set blocks.
4. `cd pft-rule-checker && npx tsc --noEmit` (or `npm run typecheck`) passes.
5. Sanity-check the math with a fixture: equity=10000, margin=2500 → currentMarginUsedPercent = 25.0. equity=0 → 0. margin=undefined → 0.
  </verify>
  <done>
- Every tick of updateRuleStateRT writes both fields to accountrulestates.
- Math.max ratchet preserves peak across ticks.
- Zero-equity / missing-margin edge cases return 0 (no NaN/Infinity).
- All three reset paths (payout, EOD, daily scheduler) explicitly DO NOT zero peakMarginUsedPercent.
- pft-backend reads the fields via its existing strict:false model — verified by manually inspecting `pft-backend/src/app/models/accountRuleState.model.ts` shows `strict: false` (no code change there).
- SUMMARY records: files touched, the EOD-exclusion audit one-liner, operational note that pft-backend needs zero change AND that Bob's "enable Risk Intelligence for backoffice" item is a Super Admin per-brand pagePermissions check (NOT a code change — see RESEARCH §Pitfall 5; ops/Bob action, not this plan).
  </done>
</task>

</tasks>

<verification>
- `grep -n "peakMarginUsedPercent\|currentMarginUsedPercent" pft-rule-checker/src/app/models/accountRuleState.model.ts pft-rule-checker/src/app/models/accountRuleState.interface.ts pft-rule-checker/src/app/services/rule-engine/ruleStateService.ts` shows expected additions only.
- `grep -rn "peakMarginUsedPercent" pft-rule-checker/src/app/services/rule-engine/eodService.ts pft-rule-checker/src/app/services/rule-engine/dailyResetScheduler.ts` shows ZERO matches (peak NOT in any daily-reset $set).
- `cd pft-rule-checker && npx tsc --noEmit` clean.
- `grep -n "strict: false" pft-backend/src/app/models/accountRuleState.model.ts` confirms zero backend change needed.
</verification>

<success_criteria>
- accountrulestates schema carries the two new fields; default 0; no index changes.
- ruleStateService writes both fields on every tick at the existing peak-block site; ratchet via Math.max preserves peak across ticks.
- Zero-equity / missing-margin edge cases yield 0 (no NaN).
- Daily/EOD/payout reset paths verified to NOT zero peakMarginUsedPercent.
- Typecheck clean; pft-backend zero code change.
- SUMMARY captures EOD-exclusion audit + non-code operational note re: Super Admin pagePermissions check for the Risk Intelligence backoffice visibility item.
</success_criteria>

<output>
After completion, create `.planning/phases/07-used-margin-display/07-01-SUMMARY.md` using the dense single-line update pattern from 04-04 / 05-01 / 06-01 SUMMARYs. Include:
- Files modified (exact paths + line ranges)
- EOD-exclusion audit one-liner (eodService.ts + dailyResetScheduler.ts + payout-reset block) confirming peak is preserved
- Operational note: pft-backend zero change (strict:false passthrough); Risk Intelligence backoffice visibility = Super Admin per-brand pagePermissions check (RESEARCH §Pitfall 5), NOT a code change — flagged for Bob/ops
- Deploy ordering reminder: rule-checker MUST deploy first; 07-02 dashboard widget will read 0 peak until at least one tick lands after rule-checker rollout
</output>
