---
phase: 07-used-margin-display
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/MarginUsageCard.tsx
  - pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx
  - pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/types.ts
  - pft-dashboard/messages/en.json
autonomous: false  # deferred post-deploy human-verify checkpoint

must_haves:
  truths:
    - "Trader account view (client) at /accounts/[id]/statistics/[mtacc] shows a graphical MarginUsageCard with current % and an all-time peak %"
    - "Admin account view at /admin/users/[id]/programs/[programId]/account/[accountId] shows the SAME widget rendered by the SAME component (TradingDashboardShared → AccountInfoSection) — NO role conditional"
    - "Current % comes from the existing accountInfo socket payload (no new polling, no new endpoint) — computed as margin/equity*100, NOT MT5 marginLevel"
    - "Peak % comes from accountrulestates.peakMarginUsedPercent (persisted by 07-01) via the existing accountRuleState read path the dashboard already uses — strict:false passthrough on pft-backend surfaces it with ZERO backend code change"
    - "Tooltip on the card explains denominator is equity, peak is all-time, no positions open => '—'"
    - "Edge cases handled: equity<=0 or margin==0 → render '—' (not NaN%, not 0.0%)"
    - "Same widget for both client + admin — verified visually on both routes post-deploy"
  artifacts:
    - path: "pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/MarginUsageCard.tsx"
      provides: "New card component: takes {currentMarginUsedPct, peakMarginUsedPct} props, renders shadcn Progress bar + bold % + peak tick + tooltip"
      min_lines: 40
    - path: "pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx"
      provides: "Mounts MarginUsageCard next to ConsistencyScoreCard; wires currentMarginUsedPct from accountInfo + peakMarginUsedPct from accountRuleState"
      contains: "MarginUsageCard"
    - path: "pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/types.ts"
      provides: "peakMarginUsedPercent + currentMarginUsedPercent added to the AccountRuleState type (mirrors strict:false backend passthrough)"
      contains: "peakMarginUsedPercent"
    - path: "pft-dashboard/messages/en.json"
      provides: "Translation keys: marginUsage.title / marginUsage.current / marginUsage.peak / marginUsage.tooltip / marginUsage.noPositions"
      contains: "marginUsage"
  key_links:
    - from: "AccountInfoSection.tsx"
      to: "accountInfo.margin / accountInfo.equity (live socket)"
      via: "useTradingDashboardData() return value already exposed"
      pattern: "accountInfo\\.(margin|equity)"
    - from: "AccountInfoSection.tsx"
      to: "accountRuleState.peakMarginUsedPercent (persisted)"
      via: "existing dashboard read path the component already consumes for peakTotalDrawdownPercent et al"
      pattern: "peakMarginUsedPercent"
    - from: "MarginUsageCard.tsx"
      to: "shadcn Progress + lucide AlertTriangle/ShieldCheck"
      via: "render"
      pattern: "Progress|AlertTriangle"
---

<objective>
Add a single MarginUsageCard component to TradingDashboardShared (via AccountInfoSection) that displays:
- Current margin used % = margin/equity*100 (live from existing accountInfo socket — zero new backend)
- All-time peak margin used % = accountrulestates.peakMarginUsedPercent (persisted by 07-01)
- Tooltip explaining denominator + peak semantics
- Color flip to AlertTriangle styling at >80% current

Because both client (`/accounts/[id]/statistics/[mtacc]`) and admin (`/admin/users/.../account/[accountId]`) routes mount the same TradingDashboardShared → AccountInfoSection, a single component edit lights up BOTH views. NO role conditional.

Purpose: Closes the Trading Cult ticket (cmovizb320007qs0k0fue250p) — give ops + traders visibility into "all-in" / high-risk margin utilization before it becomes a breach.

Output: 1 new component + 1 modified section + ~6 lines in types.ts + 5 i18n keys + 1 deferred post-deploy human-verify checkpoint.
</objective>

<constraints>
**LOCKED DESIGN — strict:false passthrough on pft-backend's `AccountRuleState` model.**

07-01 declared that the new fields (`peakMarginUsedPercent`, `currentMarginUsedPercent`) flow from rule-checker → Mongo → pft-backend read path → dashboard automatically because pft-backend's `accountRuleStateSchema` is `strict: false` (research §Pattern 3).

**`files_modified` for this plan declares ZERO pft-backend files. This is intentional and final.**

If, at execute time, Task 0 verification (grep below) shows the dashboard read path does NOT receive the new fields end-to-end (e.g. an explicit projection / DTO is filtering them out), the executor MUST STOP and surface this as a BLOCKER on the plan. The executor MUST NOT silently add a pft-backend projection extension or any other backend edit — that would violate the declared `files_modified` scope. Surface it; let revision-mode re-plan if backend work is genuinely needed.
</constraints>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-used-margin-display/07-RESEARCH.md
@.planning/phases/07-used-margin-display/07-01-PLAN.md

# Component mount points (BOTH routes load the same TradingDashboardShared)
@pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/TradingDashboardShared.tsx
@pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx
@pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/ConsistencyScoreCard.tsx

# Hook delivering live margin from existing socket
@pft-dashboard/src/hooks/useTradingDashboardData.ts
@pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/types.ts

# Confirm strict:false on backend (read-only — DO NOT modify)
@pft-backend/src/app/models/accountRuleState.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Verify strict:false passthrough actually delivers the new fields to the dashboard read path</name>
  <files>(read-only verification — modifies nothing)</files>
  <action>
Before ANY component work, confirm 07-01's strict:false passthrough assumption holds end-to-end.

1. Confirm pft-backend's AccountRuleState model is strict:false:
   ```bash
   grep -n "strict\s*:" pft-backend/src/app/models/accountRuleState.model.ts
   ```
   Expected: a line with `strict: false`. If missing → BLOCKER (07-01 premise wrong).

2. Trace the dashboard's accountRuleState read path. Find the pft-backend controller/service/route that returns the rule-state shape consumed by the dashboard (start from the endpoint hit by `useTradingDashboardData` or whichever hook feeds AccountInfoSection's rule-state prop). Grep for explicit field projections or DTO maps that could STRIP unknown fields:
   ```bash
   grep -rn "peakTotalDrawdownPercent\|highestEquityEver" pft-backend/src/app/controllers pft-backend/src/app/services pft-backend/src/app/routes 2>/dev/null | head -50
   ```
   For each hit, inspect whether the response is built via:
   - **(a) Direct pass-through** (e.g. `res.json(ruleState)` or `return ruleState.toObject()` or `.lean()` straight through) → strict:false passthrough WILL deliver the new fields. PROCEED.
   - **(b) Explicit field whitelist / projection / DTO** (e.g. `.select("peakTotalDrawdownPercent ...")` or `{ peakTotalDrawdownPercent: rs.peakTotalDrawdownPercent, ... }`) → new fields will be STRIPPED. STOP — this is a blocker.

3. Decision:
   - **All paths are (a) pass-through** → proceed to Task 1.
   - **Any path is (b) explicit whitelist** → STOP. Write `.planning/phases/07-used-margin-display/07-02-BLOCKER.md` describing: which file/line strips the field, why strict:false alone is insufficient, and a recommendation for revision (likely: add the backend file to a revised `files_modified` and add a one-line projection extension). DO NOT silently edit pft-backend. Return to orchestrator.
  </action>
  <verify>
1. `grep -n "strict\s*:" pft-backend/src/app/models/accountRuleState.model.ts` shows `strict: false`.
2. The audit of read-path files shows either no explicit field whitelist OR a BLOCKER doc has been written.
3. SUMMARY (when written at end of plan) records the read-path file(s) audited and which mode (a/b) each is in.
  </verify>
  <done>
- Either: confirmed all rule-state read paths are pass-through and Task 1 may begin.
- Or: 07-02-BLOCKER.md exists describing the projection that strips the fields, and the plan halts pending revision.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create MarginUsageCard component + add types</name>
  <files>
pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/MarginUsageCard.tsx
pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/types.ts
pft-dashboard/messages/en.json
  </files>
  <action>
1. **Create MarginUsageCard.tsx** as a new client component in the programs-details folder. Match the visual footprint of `ConsistencyScoreCard.tsx` (read it first — same Card/Progress/lucide-icon shape, same Tailwind classes, same i18n pattern via next-intl `useTranslations`).

   Props:
   ```typescript
   interface MarginUsageCardProps {
     currentMarginUsedPct: number | null;  // null when no positions / equity unknown → render "—"
     peakMarginUsedPct: number | null;     // null while peak hasn't populated yet → render "—"
   }
   ```

   Render structure:
   - `<Card>` wrapper matching ConsistencyScoreCard
   - Title: `t("marginUsage.title")` ("Margin Usage") + lucide icon (Activity or ShieldCheck for <80%, AlertTriangle for >=80%)
   - Big bold current % (or "—" if null), e.g. `25.4%`
   - shadcn `<Progress value={currentMarginUsedPct ?? 0} />` bar
   - Below the bar: small text "Peak: {peakMarginUsedPct?.toFixed(1)}%" or "—"
   - Tooltip (use existing Tooltip primitive from `@/components/ui/tooltip` — match ConsistencyScoreCard's tooltip pattern) on the title/icon explaining: "Current = open margin / equity × 100. Peak = all-time highest value since this account was created. Shown as '—' when no positions are open."
   - Color thresholds: <50% green, 50-80% amber, >=80% red (use existing Tailwind palette classes already in this folder — `text-green-500`, `text-amber-500`, `text-red-500`, or whatever ConsistencyScoreCard uses for its bands; mirror that exact palette).

   Edge cases:
   - `currentMarginUsedPct === null` → render "—" for the big number AND set bar value to 0
   - `peakMarginUsedPct === null || peakMarginUsedPct === 0` → render peak label as "—" (not "0.0%") — peak being 0 means rule-checker hasn't ticked yet post-07-01 deploy
   - NEVER render `NaN%` or `Infinity%`

2. **Extend types.ts** — find the `AccountRuleState`-shaped type (or whatever shape the dashboard uses for the persisted rule-state read; look near the existing `peakTotalDrawdownPercent` typing). Add:
   ```typescript
   peakMarginUsedPercent?: number;
   currentMarginUsedPercent?: number;
   ```
   Optional because they only populate after 07-01 deploy + first tick.

3. **Add i18n keys** to `pft-dashboard/messages/en.json` under a new `marginUsage` namespace:
   ```json
   "marginUsage": {
     "title": "Margin Usage",
     "current": "Current",
     "peak": "Peak",
     "tooltip": "Current = open margin / equity × 100. Peak = all-time highest value since this account was created. Shown as '—' when no positions are open.",
     "noPositions": "—"
   }
   ```
   Other locale files (ja.json etc.) — leave for follow-up i18n; next-intl falls back to en by default.

NO role conditional inside MarginUsageCard — it must render identically for client and admin.
  </action>
  <verify>
1. `cat pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/MarginUsageCard.tsx` shows: client component, props `currentMarginUsedPct: number | null` + `peakMarginUsedPct: number | null`, Card + Progress + Tooltip, "—" branch for nulls, color thresholds.
2. `grep -n "peakMarginUsedPercent\|currentMarginUsedPercent" pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/types.ts` shows both fields added as optional numbers.
3. `grep -n '"marginUsage"' pft-dashboard/messages/en.json` shows the new namespace with 5 keys.
4. `cd pft-dashboard && npx tsc --noEmit` (or `npm run typecheck`) passes.
5. Manually verify rendering math on a fixture: `{currentMarginUsedPct: 25.4, peakMarginUsedPct: 42.7}` renders "25.4%" big number, bar at 25%, "Peak: 42.7%" label.
  </verify>
  <done>
- MarginUsageCard.tsx exists, fully typed, renders "—" branch for nulls, no NaN/Infinity possible.
- types.ts has the two optional fields on the rule-state shape.
- en.json has the 5 new keys.
- Typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount MarginUsageCard in AccountInfoSection + wire data sources</name>
  <files>pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx</files>
  <action>
1. Open `AccountInfoSection.tsx`. Locate where `ConsistencyScoreCard` is rendered inside the section's grid. Add `MarginUsageCard` adjacent to it (same grid cell pattern, same parent grid — matches research §Recommended UI Placement).

2. **Wire `currentMarginUsedPct`** from the live socket. AccountInfoSection already receives (or has access via context/hook to) `accountInfo` from `useTradingDashboardData`. Verify by reading the component and finding the `accountInfo` reference. Compute:
   ```typescript
   const currentMarginUsedPct =
     accountInfo?.equity && accountInfo.equity > 0 && accountInfo.margin && accountInfo.margin > 0
       ? (accountInfo.margin / accountInfo.equity) * 100
       : null;  // null → MarginUsageCard renders "—"
   ```
   CRITICAL — use `margin / equity * 100`, NOT `accountInfo.marginLevel` (research §Pitfall 1 — marginLevel is the inverse).

3. **Wire `peakMarginUsedPct`** from the persisted rule-state shape AccountInfoSection already consumes. Find where `peakTotalDrawdownPercent` (or any other `accountrulestates`-sourced field) is read in this component (or its parent TradingDashboardShared). Same source delivers `peakMarginUsedPercent` (extended in types.ts in Task 1; backend passes it through via strict:false — verified end-to-end in Task 0).
   ```typescript
   const peakMarginUsedPct =
     typeof ruleState?.peakMarginUsedPercent === "number" && ruleState.peakMarginUsedPercent > 0
       ? ruleState.peakMarginUsedPercent
       : null;  // null → "—" until rule-checker has ticked at least once post-07-01 deploy
   ```

4. Pass props:
   ```tsx
   <MarginUsageCard
     currentMarginUsedPct={currentMarginUsedPct}
     peakMarginUsedPct={peakMarginUsedPct}
   />
   ```

NO role conditional. NO separate admin-only or client-only mount. The component is mounted unconditionally inside AccountInfoSection — which is itself unconditionally rendered by TradingDashboardShared on BOTH the client `/accounts/[id]/statistics/[mtacc]` route AND the admin `/admin/users/.../account/[accountId]` route.

**Locked scope reminder (see <constraints>):** ZERO pft-backend edits permitted in this plan. If the wiring fails because `ruleState.peakMarginUsedPercent` arrives undefined despite 07-01 having deployed and ticked, STOP and write `07-02-BLOCKER.md` — do not silently extend a backend projection.
  </action>
  <verify>
1. `grep -n "MarginUsageCard" pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx` shows the import + a single render site (no role-conditional wrapper).
2. `grep -n "accountInfo.margin\|accountInfo\\?.margin" pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx` shows the `margin / equity * 100` computation (NOT `marginLevel`).
3. `grep -n "peakMarginUsedPercent" pft-dashboard/src/app/(dashboard)/_components/modules/users/programs-details/AccountInfoSection.tsx` shows the peak read from the rule-state shape.
4. `cd pft-dashboard && npm run dev` boots; visit both routes locally with a test account:
   - `/accounts/{userId}/statistics/{mtacc}` (client view) — MarginUsageCard renders
   - `/admin/users/{userId}/programs/{programId}/account/{accountId}` (admin view) — MarginUsageCard renders, identical visual
5. Typecheck clean: `cd pft-dashboard && npx tsc --noEmit`.
  </verify>
  <done>
- MarginUsageCard mounted inside AccountInfoSection — single render site, no role conditional.
- Current pct = margin/equity*100 from live accountInfo socket (NOT marginLevel).
- Peak pct = ruleState.peakMarginUsedPercent from existing accountrulestates read path (strict:false passthrough confirmed in Task 0 — ZERO backend edit).
- Both routes render the card visually with no role gating.
- Typecheck clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Deferred post-deploy human-verify on Trading Cult</name>
  <what-built>
07-01 persists currentMarginUsedPercent + peakMarginUsedPercent to accountrulestates every tick (rule-checker). 07-02 renders MarginUsageCard in AccountInfoSection (one card, no role conditional) showing current from the live socket + peak from accountrulestates. Same card on BOTH client and admin account views.
  </what-built>
  <how-to-verify>
**Wait for deploy.** Order: pft-rule-checker main-2026 → pft-dashboard main-2026. (No pft-backend deploy needed — strict:false passthrough verified in Task 0.) Allow ~5-10 minutes after rule-checker deploys for at least one tick to land on a live MLM-eligible Trading Cult account.

1. Pick an active Trading Cult MT5 account with open positions (the support team or Bob can name one — ideally an account known to run high margin).

2. **Client view:**
   - Visit `https://dash.tradingcult.com/accounts/{userId}/statistics/{mtacc}` as the trader
   - Confirm MarginUsageCard renders inside the Account Info section (adjacent to ConsistencyScoreCard)
   - Confirm current % updates live as positions change (open a small position, watch the bar move)
   - Hover the title/icon — tooltip explains "margin / equity × 100" and "peak is all-time"
   - With NO positions open: card shows "—" (not "0.0%", not "NaN%")

3. **Admin view:**
   - Visit `https://dash.tradingcult.com/admin/users/{userId}/programs/{programId}/account/{mtacc}` as admin/backoffice
   - Confirm the SAME MarginUsageCard renders identically — same position, same colors, same tooltip
   - Confirm there is NO admin-only / client-only difference in the card itself

4. **Peak ratchet:**
   - Open a larger position so current % exceeds the previous peak; refresh the page; confirm peak % updates to match the new high
   - Close all positions; confirm peak STAYS at the higher value (peak is all-time, NOT reset)
   - Wait until next daily reset (or trigger one manually if you have access); confirm peak STAYS at the higher value (peak NOT zeroed on EOD)

5. **Edge cases:**
   - Account with zero open positions: card shows "—" and "—"
   - Account fresh after deploy (no peak history): peak shows "—" until first tick

**Separately (operational note — NOT a code task — flag this to Bob/ops):**
- Verify Trading Cult's Super Admin per-brand pagePermissions for `/admin/risk-intelligence/*`, `/admin/users/suspicious-accounts`, `/admin/users/fraud-check` permit the backOffice role. If backoffice users can't see Risk Intelligence in the sidebar despite the code allowing it, this Super Admin config is the cause (RESEARCH §Pitfall 5). NO code change here.

Report any of:
- Card missing on either route
- Current % wrong direction (e.g. shows 350% when expected ~5% — likely marginLevel/marginUsed flip)
- Peak resets to 0 on daily/EOD reset
- NaN%, Infinity%, or 0.0% rendered when "—" expected
- Visual difference between client and admin views
  </how-to-verify>
  <resume-signal>Type "approved" once visual + functional + peak-ratchet checks pass on a live Trading Cult account, OR describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Task 0 confirmed strict:false passthrough delivers new fields end-to-end (OR a 07-02-BLOCKER.md exists and the plan halted).
- MarginUsageCard.tsx exists with null-safe rendering and no NaN/Infinity paths.
- AccountInfoSection.tsx mounts it once, no role conditional, current from `margin/equity*100` (NOT marginLevel), peak from accountrulestates.
- en.json has 5 marginUsage keys.
- types.ts carries the two optional fields.
- Typecheck clean.
- Zero files in pft-backend modified (declared scope honored).
- Both client and admin routes verified post-deploy on Trading Cult.
</verification>

<success_criteria>
- Single card component renders identically on both surfaces.
- Current margin used % updates within existing socket cadence (no new polling).
- Peak ratchets up only, never decreases, never resets on EOD/daily/payout (verified by 07-01 audit + post-deploy human-verify).
- Tooltip explains denominator + peak semantics.
- Edge cases (no positions, fresh account, zero equity) render "—" not garbage.
- Zero pft-backend edits (strict:false passthrough is the locked design — Task 0 confirms or blocks).
- Zero changes to pfr-super-admin code (Risk Intelligence backoffice visibility = Super Admin operational config flagged for Bob).
- SUMMARY records: files modified, Task 0 passthrough-audit result (which read-path files were inspected and confirmed pass-through), deploy ordering, and the non-code Super Admin pagePermissions follow-up for Bob.
</success_criteria>

<output>
After completion, create `.planning/phases/07-used-margin-display/07-02-SUMMARY.md` using the dense single-line update pattern from 04-04 / 05-01 / 06-01 SUMMARYs. Include:
- Files modified (exact paths) — confirm ZERO pft-backend files
- Task 0 passthrough-audit result: list of read-path files inspected + confirmation each is mode (a) pass-through
- Deploy ordering executed (rule-checker → dashboard; NO backend deploy)
- Post-deploy human-verify result (or "deferred" if the deploy hasn't happened yet — note the deferred-human-verify convention from 04-04/05-01/06-01)
- Operational follow-up flagged for Bob: Super Admin per-brand pagePermissions for Risk Intelligence backoffice access on Trading Cult (NOT a code change)
</output>
