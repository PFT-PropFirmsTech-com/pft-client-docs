---
phase: 10-capture-persist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-backend/src/app/modules/Auth/auth.interface.ts
  - pft-backend/src/app/modules/Auth/auth.model.ts
autonomous: true

must_haves:
  truths:
    - "A user who registers after visiting a tracking link has partnerClickId: 'ABC123' stored on their User document"
    - "The partnerClickId survives the two-step OTP flow (temp user at initiateRegistration → verifyRegistrationOtp) unchanged"
    - "A user who registers with no tracking link has no partnerClickId field on their User document (skip-when-absent)"
  artifacts:
    - path: "pft-backend/src/app/modules/Auth/auth.interface.ts"
      provides: "partnerClickId?: string on TRegisterUser"
      contains: "partnerClickId"
    - path: "pft-backend/src/app/modules/Auth/auth.model.ts"
      provides: "partnerClickId field on UserSchema (indexed)"
      contains: "partnerClickId"
  key_links:
    - from: "pft-backend/src/app/modules/Auth/auth.model.ts UserSchema"
      to: "registration payload spread ({ ...payload } → new User) at auth.service.ts:721-743 and :560"
      via: "Mongoose persists the field because it now exists on the schema (strict mode)"
      pattern: "partnerClickId:\\s*\\{\\s*type:\\s*String"
---

<objective>
CRM-02: Persist the partner `clickid` UNCHANGED on the User document at registration, so it survives from click → account and every later event emitter (Phase 11) can read it off the user doc. Add `partnerClickId` to the `TRegisterUser` interface and an indexed `partnerClickId` field to `UserSchema`.

Purpose: The registration service already spreads the entire incoming body onto the User doc (`const userData = { ...payload }` → `new User({ ...userData })`). Under Mongoose strict mode, an unknown body field is silently dropped — so the ONLY thing required for persistence is that the schema field exists. Once it does, `partnerClickId` (forwarded by plan 10-01's signup body) rides through automatically, exactly like the existing `preferredLanguage` field does. Skip-when-absent is automatic: no field in the body ⇒ nothing spread ⇒ field stays undefined.

Output: `partnerClickId` typed on `TRegisterUser` and stored (indexed) on the User model.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-capture-persist/10-RESEARCH.md

# Backend — interface + model to extend; service is READ-ONLY reference (proves the spread persists it)
@pft-backend/src/app/modules/Auth/auth.interface.ts
@pft-backend/src/app/modules/Auth/auth.model.ts
</context>

<constraints>
- pft-backend commits to branch `main-2026` (`main` is DEPRECATED). Commit task MUST run `git branch --show-current` first → confirm `main-2026`.
- BEFORE editing: `git fetch origin main-2026`; confirm no upstream `partnerClickId` addition arrived since planning (verified absent). Defer to remote if it did.
- Zero new npm deps. Brand separation is per-DB — do NOT add a brandId to the field or schema.
- Value stored UNCHANGED (no transform/trim/lowercase) — the partner requires byte-identical echo-back in Phase 12.
- Backend full `tsc` OOMs (exit 134). Use ONLY scoped `npx tsc --noEmit --skipLibCheck` on the two edited files. If it still OOMs, rely on CI.
- NO service-code change is required in auth.service.ts. RESOLVED during planning (do not re-litigate): `initiateRegistration` builds the temp User via `const userData: any = { ...payload }` then `new User({ ...userData, ... })` (auth.service.ts:721-743); the one-step (2FA-disabled) path does the same at :554-576; and `verifyRegistrationOtp`'s completion `findByIdAndUpdate` (auth.service.ts:830-840) only sets referralCode/role/isRegistered/$unset otp — it does NOT touch or clobber `partnerClickId`. Therefore adding the schema field is sufficient for persistence AND survival across the OTP round-trip. Do NOT edit auth.service.ts.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add partnerClickId to TRegisterUser interface + indexed field on UserSchema (CRM-02)</name>
  <files>pft-backend/src/app/modules/Auth/auth.interface.ts, pft-backend/src/app/modules/Auth/auth.model.ts</files>
  <action>
1) pft-backend/src/app/modules/Auth/auth.interface.ts — in `interface TRegisterUser` (starts line 320), add alongside the existing optional fields (place it near `preferredLanguage?` at line 338, the established precedent for a body-spread field). Add with a short comment:
   `// Arbitrary partner click identifier captured at a tracking-link landing`
   `// (_partner_clickid cookie, forwarded in the signup body). Persisted UNCHANGED`
   `// for later S2S postback echo-back. Absent for organic signups.`
   `partnerClickId?: string;`

2) pft-backend/src/app/modules/Auth/auth.model.ts — add a `partnerClickId` field to `UserSchema`. Mirror the existing simple string fields; place it near `preferredLanguage` (schema line ~530, the same precedent). Make it indexed (research CRM-02: needed for later lookups + support queries), no default (skip-when-absent must leave it undefined, NOT ""):
   `partnerClickId: {`
   `  type: String,`
   `  index: true,`
   `},`
   Do NOT add `default: ""`, `trim: true`, `lowercase: true`, or `required` — the value must persist byte-identical and be genuinely absent when not provided.

Do NOT edit auth.service.ts (persistence is via the existing payload spread — see constraints).
  </action>
  <verify>
Scoped typecheck (NOT full tsc): `cd pft-backend && npx tsc --noEmit --skipLibCheck src/app/modules/Auth/auth.interface.ts src/app/modules/Auth/auth.model.ts` — if OOM (134), note and rely on CI.
Grep: `grep -n "partnerClickId" src/app/modules/Auth/auth.interface.ts src/app/modules/Auth/auth.model.ts` shows the interface field and the indexed schema field.
Reasoning check: because the schema field now exists, `new User({ ...userData })` in initiateRegistration (line 728) will persist `partnerClickId` when the body carries it; Mongoose strict mode drops it otherwise (skip-when-absent).
  </verify>
  <done>`TRegisterUser.partnerClickId?: string` exists; `UserSchema` has an indexed `partnerClickId` String field with no default; auth.service.ts untouched. A registration body carrying partnerClickId will now persist it on the User doc (via the existing spread), and one without it leaves the field undefined.</done>
</task>

<task type="auto">
  <name>Task 2: Commit + push User partnerClickId persistence to main-2026</name>
  <files>pft-backend/src/app/modules/Auth/auth.interface.ts, pft-backend/src/app/modules/Auth/auth.model.ts</files>
  <action>
In pft-backend: `git branch --show-current` MUST be `main-2026` (STOP if not).
`git fetch origin main-2026`; re-confirm no upstream `partnerClickId` User change landed since planning (`git grep -l partnerClickId origin/main-2026`); if it did, defer to remote and skip.
Stage ONLY the two auth files, commit, push:
  `git add src/app/modules/Auth/auth.interface.ts src/app/modules/Auth/auth.model.ts`
  `git commit` message: `feat(10-02): persist partnerClickId on User at registration (CRM-02)` — end with the Co-Authored-By trailer.
  `git push origin main-2026`
  </action>
  <verify>`git log -1 --oneline` shows feat(10-02) on main-2026; `git status` clean for the two files; `git rev-parse HEAD` == `git rev-parse origin/main-2026`.</verify>
  <done>User `partnerClickId` schema+interface committed and pushed to `main-2026` (or deferred to an equivalent remote commit).</done>
</task>

</tasks>

<verification>
- `TRegisterUser` includes `partnerClickId?: string`; `UserSchema` has an indexed `partnerClickId` string field with no default.
- Data-path reasoning: a `/auth/register` body with `partnerClickId: "ABC123"` (from plan 10-01) → temp User created with the value → OTP verify preserves it → final User doc has `partnerClickId: "ABC123"`. A body without it → no field on the User doc.
- Commit on `main-2026`, pushed (HEAD == origin/main-2026). Scoped tsc passes (or CI relied upon).
</verification>

<success_criteria>
- CRM-02 satisfied: partner clickid persisted unchanged on the User document at registration, surviving the two-step OTP flow.
- Success Criterion 2 (ROADMAP): post-tracking-link registration → `partnerClickId: "ABC123"` on the User doc. ✓
- Success Criterion 4 (ROADMAP): registration without a tracking link → no `partnerClickId` on the User doc (skip-when-absent). ✓
</success_criteria>

<output>
After completion, create `.planning/phases/10-capture-persist/10-02-SUMMARY.md`.
</output>
