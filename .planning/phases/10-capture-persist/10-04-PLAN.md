---
phase: 10-capture-persist
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02", "10-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Live: GET /track?clickid=ABC123 on the deployed brand landing sets _partner_clickid and redirects, value unchanged"
    - "Live: a registration through a tracking link writes partnerClickId onto the User doc in the brand DB"
    - "Live: that user's first checkout writes attribution.partnerClickId onto the Payment doc at creation"
    - "Live: an organic registration (no tracking link) leaves partnerClickId absent on the User doc"
  artifacts: []
  key_links: []
---

<objective>
End-to-end live verification of the full capture→persist path (CRM-01/02/03) against a deployed `main-2026` build. This is a DEFERRED post-deploy checkpoint — it does NOT block Phase 10 code completion. Per project convention, every phase's live human-verify is gated on the next `main-2026` deploy; this plan is the placeholder for that verification and should be run after Phase 10's three code plans are deployed.

Purpose: The three code plans are proven by scoped typecheck + code reasoning, but the true acceptance (cookie set on the real landing domain, values landing in the per-brand DB) can only be confirmed against a running deployment.

Output: A signed-off confirmation (or a gap list feeding `/gsd:plan-phase 10 --gaps`).
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-capture-persist/10-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="deferred">
  <what-built>
CRM-01/02/03 across pft-backend + pft-dashboard (all on main-2026):
- `GET /api/tracking/track?clickid=…` sets a first-party `_partner_clickid` cookie (raw value) + 302 redirect (10-01).
- pft-dashboard signup forwards `_partner_clickid` as `partnerClickId` in the register body (10-01).
- `partnerClickId` persisted (indexed) on the User doc at registration (10-02).
- `partnerClickId` persisted on the Payment doc `attribution` at standard checkout AND PAP funded-leg creation, from the authoritative User-doc value (10-03).
  </what-built>
  <how-to-verify>
DEFERRED until the next main-2026 deploy reaches the target brand (Trading Cult is the intended partner brand; the plumbing is brand-agnostic). Then, against the deployed environment:

1. Cookie + redirect: In a browser, hit `https://<brand-landing>/track?clickid=ABC123` (or `/api/tracking/track?clickid=ABC123` if the landing has not yet been pointed at the route). Confirm: a `_partner_clickid` cookie is set with value exactly `ABC123` (DevTools → Application → Cookies — NOT url-encoded), and the page 302-redirects to `/`. Try `?clickid=a+b/c=d` and confirm the cookie holds the raw value byte-for-byte.
2. User persistence: From that same browser (cookie present), complete a registration (through OTP if 2FA is on). In the brand DB, load the new User doc and confirm `partnerClickId: "ABC123"`.
3. Payment persistence: As that user, start a checkout (standard challenge). Confirm the created Payment doc has `attribution.partnerClickId: "ABC123"` at creation (before any gateway callback). If feasible, also verify a PAP funded-leg payment carries it.
4. Skip-when-absent: In a fresh browser with NO `_partner_clickid` cookie, register. Confirm the new User doc has NO `partnerClickId` field (absent/undefined, not "").
  </how-to-verify>
  <resume-signal>Type "approved" if all four hold. If any fail, describe the gap (which step, observed vs expected) so it can feed `/gsd:plan-phase 10 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
All four live checks pass on the deployed brand: cookie set raw + redirect, User doc carries partnerClickId, Payment doc carries attribution.partnerClickId, organic signup omits the field.
</verification>

<success_criteria>
- ROADMAP Success Criteria 1-4 confirmed against a live deployment (or gaps captured for a follow-up --gaps plan).
- This checkpoint is DEFERRED: Phase 10 is considered code-complete once 10-01/10-02/10-03 are done + pushed; this plan closes only after the next deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/10-capture-persist/10-04-SUMMARY.md`.
</output>
