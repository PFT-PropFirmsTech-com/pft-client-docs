---
phase: 09-pap-funded-queue-state-label
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-backend/src/app/modules/Payment/payment.service.modular.ts
  - pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts
  - pft-dashboard/src/app/(dashboard)/_components/modules/admin/payments-history/PaymentsTable.tsx
  - pft-dashboard/src/app/(dashboard)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx
autonomous: false  # deferred post-deploy human-verify

must_haves:
  truths:
    - "PAP funded-leg row with a pending/processing fundedprogressionqueues entry shows the real queue-state label (Awaiting KYC / Awaiting Contract / Awaiting KYC & Contract / In Funded Queue) instead of Program Not Assigned in both the admin payments TABLE and the admin payment DETAILS view"
    - "Label reflects live compliance gate: kycApproved=false → Awaiting KYC (with contract-also-false variant Awaiting KYC & Contract); kycApproved=true && contractApproved=false → Awaiting Contract; both true → In Funded Queue"
    - "Retry + Mark Done buttons are hidden on rows/details that show a queue-state label (support cannot trigger a useless retry loop on a compliance-gated row)"
    - "PAP funded-leg row with NO matching queue entry still renders the existing Program Not Assigned + Retry + Mark Done UI unchanged"
    - "Non-PAP payment rows are unaffected — no queue lookup surfaces new UI and existing layout is preserved"
    - "getPaymentHistoryAdmin attaches fundedDeferral only via a single paymentId-keyed $in batch join per page (no N+1); skipEnrichment=true (CSV export) skips the queue join"
    - "Most-recent queue entry per paymentId wins when multiple rows exist (sort createdAt desc + first-wins mirror of user-facing getPaymentHistory)"
    - "The join gate is fundedDeferral presence + status in {pending,processing}; programAssigned field is NOT re-used as the source of truth (diagnostic payment 6a2c08b1ab4caef5631099a2 has programAssigned=true while the queue is pending)"
    - "FundedProgressionQueue collection has a sparse index on paymentId to keep the $in batch join fast as the collection grows"
    - "Deploys via main-2026 only — no rule-checker, super-admin, or brand-backend-only-branch changes; no database migration required beyond the auto-built index"
  artifacts:
    - path: "pft-backend/src/app/modules/Payment/payment.service.modular.ts"
      provides: "getPaymentHistoryAdmin enriched with paymentId-keyed FundedProgressionQueue batch join, attaching fundedDeferral per row"
      contains: "paymentIdToDeferral"
    - path: "pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts"
      provides: "Sparse index on paymentId for the new admin batch join"
      contains: "paymentId: 1"
    - path: "pft-dashboard/src/app/(dashboard)/_components/modules/admin/payments-history/PaymentsTable.tsx"
      provides: "ProgramAssignmentBadge branches on fundedDeferral to render queue-state label + hide Retry/Mark Done"
      contains: "fundedDeferral"
    - path: "pft-dashboard/src/app/(dashboard)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx"
      provides: "Action Required card branches on fundedDeferral to render queue-state informational card + hide Retry/Mark Done"
      contains: "fundedDeferral"
  key_links:
    - from: "getPaymentHistoryAdmin enriched map"
      to: "FundedProgressionQueue.find({ paymentId: { $in: pageIds } })"
      via: "dynamic import + $in batch join keyed by payment._id"
      pattern: "FundedProgressionQueue\\.find.*paymentId.*\\$in"
    - from: "PaymentsTable ProgramAssignmentBadge"
      to: "payment.fundedDeferral"
      via: "conditional inside the payment.programAssigned === false outer guard"
      pattern: "fundedDeferral"
    - from: "PaymentDetailsContainer Action Required card"
      to: "payment.fundedDeferral"
      via: "conditional inside the payment.programAssigned === false outer guard"
      pattern: "fundedDeferral"
    - from: "shared queue-state label helper"
      to: "kycApproved / contractApproved / status flags on fundedDeferral"
      via: "getQueueStateLabel(deferral) pure function"
      pattern: "Awaiting KYC|Awaiting Contract|In Funded Queue"
---

<objective>
Replace the misleading "Program Not Assigned" warning that fires on PAP funded-leg admin payment rows whose users are actually just stuck in the KYC/contract queue. The rule-checker + queue cron already provision the account automatically once compliance clears; the UI has been telling support the opposite ("system failure — click Retry"), causing bad retries and confused ops replies to clients.

The fix is a narrow port of an already-proven pattern: the user-facing `getPaymentHistory` already batch-joins `FundedProgressionQueue` by `paymentId` and attaches `fundedDeferral` per row. The user-facing dashboard already reads it (`getFundedHoldLabel`). Only the admin-facing endpoint + two admin components are missing the wiring. `PaymentData.fundedDeferral` is already declared in the dashboard TypeScript type — no type change needed.

Purpose: Close ticket cmqbzq6vc007ds50k008tr3du (DEV — PAP funded queue gate) and stop the recurring "Program Not Assigned = broken" misdiagnosis in support workflows. Preserves existing UI for genuine failures (no queue entry → old label + buttons).

Output: Backend enrichment + sparse index + two admin components branching on fundedDeferral, pushed to main-2026 across pft-backend and pft-dashboard.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pap-funded-queue-state-label/09-RESEARCH.md

@pft-backend/src/app/modules/Payment/payment.service.modular.ts
@pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts
@pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.interface.ts
@pft-dashboard/src/hooks/usePayments.ts
@pft-dashboard/src/app/(dashboard)/_components/modules/admin/payments-history/PaymentsTable.tsx
@pft-dashboard/src/app/(dashboard)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx
@pft-dashboard/src/app/(dashboard)/_components/modules/users/payment-history/PaymentHistoryContainer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — enrich getPaymentHistoryAdmin with fundedDeferral batch join + add sparse paymentId index</name>
  <files>pft-backend/src/app/modules/Payment/payment.service.modular.ts, pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts</files>
  <action>
**Pre-work (MANDATORY per MEMORY.md `feedback_rebase_when_remote_already_fixed.md`):**

```
cd pft-backend
git fetch origin main-2026
git status                       # working tree must be clean or scoped to this plan only
git branch --show-current        # MUST print main-2026 (HARD RULE per MEMORY.md)
git log --oneline origin/main-2026 -20 | grep -i -E "fundedDeferral|getPaymentHistoryAdmin|paymentId.*index" || echo "no remote overlap"
```
If the remote grep shows any hit indicating this work already landed (unlikely given ticket state), STOP and re-scope — do not overwrite remote work.

**Part A — payment.service.modular.ts (mirror the user-facing pattern into the admin path):**

Open `pft-backend/src/app/modules/Payment/payment.service.modular.ts`.

1. Locate `getPaymentHistoryAdmin` (around line 1845). Find the `enriched = history.map(...)` block (currently around line 2044). Immediately BEFORE that `enriched = history.map` line, insert the same `paymentId`-keyed batch join used by `getPaymentHistory` (currently lines 1778–1798).

2. Copy the pattern verbatim from lines 1778–1798, adapted for the admin path:
   - Source array is `history` (not `result.data` — this endpoint uses `history` as the paginated array).
   - `pageIds` is `history.map((p: any) => String(p._id))`.
   - Same dynamic import of `FundedProgressionQueue`.
   - Same `.select("paymentId reason status kycApproved contractApproved createdAt").sort({ createdAt: -1 }).lean()`.
   - Same first-wins loop keyed by `String(e.paymentId)`.

3. In the existing `enriched = history.map((p: any) => { ... return { ...obj, user: ..., couponAffiliate: ..., referralAffiliate: ... }; })` block, add ONE new field to the returned object:
   ```
   fundedDeferral: paymentIdToDeferral[String(obj._id)] || undefined,
   ```
   Place it alongside `couponAffiliate` / `referralAffiliate` for readability.

4. **skipEnrichment guard:** `getPaymentHistoryAdmin` supports a `skipEnrichment` flag on the CSV export path — verify the code path. If `skipEnrichment=true` returns early BEFORE this new block, no change needed. If the current structure keeps the block inline, wrap the new batch join in `if (!skipEnrichment) { ... }`. The CSV export must NOT hit the queue join. Match whatever pattern the existing `couponAffiliate` / `referralAffiliate` joins use (they should already be gated the same way — copy exactly).

5. Add a leading comment on the new block explaining why (copy the intent of the user-facing comment at lines 1772–1777, adjusted for admin):
   ```
   // Admin needs the SAME fundedDeferral signal the trader sees on their history —
   // when a PAP funded-leg payment is being held by the KYC/contract queue, the admin
   // table should say "Awaiting KYC" / "Awaiting Contract" / "In Funded Queue" instead
   // of the generic "Program Not Assigned" (which used to imply a system failure).
   // Batch-join by paymentId for this page only; skipped on CSV export path.
   ```

**Part B — fundedProgressionQueue.model.ts (add sparse paymentId index):**

Open `pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts`. The existing indexes are `{ userId, status }`, `{ userId, mt5AccountId } unique`, `{ status, createdAt }` (per research).

Add ONE new schema-level index (mongoose auto-builds it on next boot — no manual migration):
```
FundedProgressionQueueSchema.index({ paymentId: 1 }, { sparse: true });
```
Place it alongside the other `.index(...)` calls. Sparse because `paymentId` is optional (older non-PAP entries may lack it). Add a one-line comment: `// Supports admin getPaymentHistoryAdmin paymentId $in batch join (Phase 9).`

**Type-check (SCOPED per MEMORY.md `reference_backend_tsc_oom.md` — full tsc will OOM):**

```
cd pft-backend
npx tsc --noEmit --skipLibCheck src/app/modules/Payment/payment.service.modular.ts src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts 2>&1 | tail -30
```
If scoped tsc still explodes, rely on CI. Do NOT run a full-project tsc.

**Commit + push:**

```
cd pft-backend
git branch --show-current    # MUST be main-2026
git add src/app/modules/Payment/payment.service.modular.ts src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts
git commit -m "fix(09-01): attach fundedDeferral to admin payments + index queue.paymentId

getPaymentHistoryAdmin now runs the same paymentId-keyed FundedProgressionQueue
batch join that the user-facing getPaymentHistory already does, populating
fundedDeferral per row. Admin dashboard branches on this to render the real
queue state (Awaiting KYC / Awaiting Contract / In Funded Queue) instead of
the misleading 'Program Not Assigned' warning on PAP funded-leg rows that
are actually just held on compliance.

Sparse index on FundedProgressionQueue.paymentId keeps the \$in batch join
fast as the collection grows (mongoose auto-builds on next boot).

skipEnrichment=true (CSV export) skips the join — matches existing pattern
for couponAffiliate/referralAffiliate joins.

Ticket: cmqbzq6vc007ds50k008tr3du (DEV — PAP funded queue gate)
Diagnostic case: NSF payment 6a2c08b1ab4caef5631099a2 (queue 6a2c0a5615385fe987c37953)"
git push origin main-2026
```
  </action>
  <verify>
- `cd pft-backend && git branch --show-current` prints `main-2026`.
- `grep -n "paymentIdToDeferral" pft-backend/src/app/modules/Payment/payment.service.modular.ts` returns at least TWO hits (one for user-facing at ~line 1778 that already exists, one NEW inside getPaymentHistoryAdmin).
- `grep -n "fundedDeferral:" pft-backend/src/app/modules/Payment/payment.service.modular.ts` returns at least TWO hits (existing at ~line 1803, new inside admin enriched.map).
- `grep -n "FundedProgressionQueue.find" pft-backend/src/app/modules/Payment/payment.service.modular.ts` returns at least TWO hits (user-facing + new admin).
- `grep -n "paymentId: 1" pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts` shows the new sparse index.
- Scoped `npx tsc --noEmit --skipLibCheck` on the two files exits 0 (or fails only on unrelated pre-existing errors — record any such delta).
- `git log -1 --format=%s origin/main-2026` shows `fix(09-01): attach fundedDeferral to admin payments + index queue.paymentId`.
- `git diff HEAD~1 -- pft-backend/src/app/modules/Payment/payment.service.modular.ts | grep -c "^+"` shows a small diff (~25-30 additions), not a rewrite.
  </verify>
  <done>
- Admin `getPaymentHistoryAdmin` returns each payment row with `fundedDeferral: { reason, status, kycApproved, contractApproved } | undefined`.
- Batch join is one `$in` query per page keyed by `paymentId` (no N+1).
- `skipEnrichment=true` path does NOT execute the queue join.
- `FundedProgressionQueue` has a sparse `{ paymentId: 1 }` index declared in the schema (auto-built on next backend boot).
- Commit pushed to `origin/main-2026`.
- Zero changes to user-facing `getPaymentHistory` (still the same pattern it was).
- Zero changes to rule-checker, super-admin, brand-backend-only branches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard — branch ProgramAssignmentBadge + Action Required card on fundedDeferral</name>
  <files>pft-dashboard/src/app/(dashboard)/_components/modules/admin/payments-history/PaymentsTable.tsx, pft-dashboard/src/app/(dashboard)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx</files>
  <action>
**Pre-work:**
```
cd pft-dashboard
git fetch origin main-2026
git status
git branch --show-current    # MUST be main-2026
git log --oneline origin/main-2026 -20 | grep -i -E "fundedDeferral.*admin|ProgramAssignmentBadge.*Awaiting" || echo "no remote overlap"
```

**Part A — Shared label helper (place in the file with the FIRST usage, do NOT create a new util file unless the repo has a natural home — inline helper is fine for a two-callsite change):**

Add this pure function at the TOP of `PaymentsTable.tsx` (module scope, above the components — file already colocates several small helpers like `AcquisitionBadge`). Match the user-facing `getFundedHoldLabel` pattern in `PaymentHistoryContainer.tsx` lines 77–86, but with hardcoded English strings (admin components do NOT use i18n — confirmed by research):

```tsx
// Derives the admin queue-state label from the fundedDeferral join attached
// server-side by getPaymentHistoryAdmin (Phase 9). Returns null when there is
// no active queue hold — caller falls back to the existing "Program Not
// Assigned" UI. Mirrors getFundedHoldLabel() in the user-facing history.
export function getQueueStateLabel(
  deferral: PaymentData["fundedDeferral"] | undefined,
): string | null {
  if (!deferral) return null;
  if (deferral.status !== "pending" && deferral.status !== "processing") return null;
  if (!deferral.kycApproved && !deferral.contractApproved) return "Awaiting KYC & Contract";
  if (!deferral.kycApproved) return "Awaiting KYC";
  if (!deferral.contractApproved) return "Awaiting Contract";
  return "In Funded Queue";
}
```

Then in `PaymentDetailsContainer.tsx`, IMPORT it: `import { getQueueStateLabel } from "../payments-history/PaymentsTable";`. If the import path is awkward or introduces a circular dep, MOVE `getQueueStateLabel` to a new small file `pft-dashboard/src/app/(dashboard)/_components/modules/admin/_shared/paymentQueueLabel.ts` and import from both sites. Pick whichever avoids circular imports — record the choice in the SUMMARY.

**Part B — PaymentsTable.tsx `ProgramAssignmentBadge` (lines ~214–303):**

Keep the outer guard exactly as-is (line 223 — `if (payment.status !== "completed" || payment.programAssigned !== false) return null;`). This preserves success criterion #4 (non-PAP rows unaffected) and #5.

Inside the returned JSX, BEFORE the existing warning header at line 228, compute the label:
```tsx
const queueLabel = getQueueStateLabel(payment.fundedDeferral);
```

Branch the JSX:

- **If `queueLabel` is a string** → render a calmer, informational badge (NOT amber-warning, use blue/indigo to signal "held, not broken"). Suppress the Retry + Mark Done buttons entirely. Suggested markup (match existing sizing / Tooltip conventions; do NOT overdesign):
  ```tsx
  return (
    <div className="mt-2.5 p-2.5 rounded-lg bg-gradient-to-r from-blue-50 to-indigo-50 dark:from-blue-500/10 dark:to-indigo-500/10 border border-blue-200 dark:border-blue-500/30">
      <div className="flex items-center gap-2">
        <div className="p-1 rounded-md bg-blue-100 dark:bg-blue-500/20">
          <Clock className="w-3.5 h-3.5 text-blue-600 dark:text-blue-400" />
        </div>
        <span className="text-xs font-semibold text-blue-700 dark:text-blue-300">
          {queueLabel}
        </span>
      </div>
    </div>
  );
  ```
  Import `Clock` from `lucide-react` alongside the existing icon imports (RefreshCw, CheckCircle, AlertTriangle — already imported at top of file).

- **Else** → render the EXISTING amber "Program Not Assigned" JSX + both buttons UNCHANGED.

**Part C — PaymentDetailsContainer.tsx "Action Required" card (line ~578 outer guard, lines 630/639 button handlers):**

Keep the outer guard exactly as-is (`payment.status === "completed" && payment.programAssigned === false`).

Inside that block, at the top, compute the label:
```tsx
const queueLabel = getQueueStateLabel(payment.fundedDeferral);
```

Branch:

- **If `queueLabel` is a string** → render an INFORMATIONAL "Funded Account On Hold" card (NOT "Action Required"). Body text: `"This funded account is waiting on {queueLabel === 'Awaiting KYC' ? 'KYC verification' : queueLabel === 'Awaiting Contract' ? 'contract signature' : queueLabel === 'Awaiting KYC & Contract' ? 'both KYC verification and contract signature' : 'the automated queue to process (KYC and contract already approved)'}."`. Include a short note: `"The funded queue will provision the account automatically once compliance clears — no manual action required here."` Use a neutral/informational card style (blue) — do NOT render the Retry or Mark Done buttons. Keep the outer card structure so screen layout is preserved.

- **Else** → render the EXISTING "Action Required: Program Not Assigned" card WITH Retry + Mark Done buttons UNCHANGED (line 630 `handleRetryAssignment`, line 639 `handleMarkAssigned`).

**Do NOT:**
- Add i18n. Admin components use hardcoded EN strings by convention (research confirms this — the user-facing container uses `t()` but no admin one does).
- Change the `PaymentData` type — `fundedDeferral` is already declared in `pft-dashboard/src/hooks/usePayments.ts`.
- Touch `useRetryProgramAssignment` / `useMarkProgramAssigned` hooks — Retry/Mark Done still work on the fallback (no-queue-entry) path.
- Change React Query cache config or `invalidateAdminPayments` — existing invalidation flow is untouched.
- Touch any non-admin component (client-facing `PaymentHistoryContainer` stays unchanged — it already had `getFundedHoldLabel`).

**Type-check (scoped):**
```
cd pft-dashboard
npx tsc --noEmit --skipLibCheck \
  src/app/\(dashboard\)/_components/modules/admin/payments-history/PaymentsTable.tsx \
  src/app/\(dashboard\)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx 2>&1 | tail -30
```
Or the repo's standard `pnpm typecheck` / `npm run typecheck` if available and doesn't OOM. Fix any type errors that come from this change; ignore pre-existing unrelated errors (record deltas in SUMMARY).

**Manual sanity check (local, only if backend is running against a DB that has the diagnostic case):**
- Load `/admin/payments` and navigate to the diagnostic user's payment. Expect: badge reads "Awaiting KYC" (kycApproved=false, contractApproved=true on the diagnostic queue entry), no Retry/Mark Done buttons.
- Load `/admin/payments/:id` for a NON-PAP payment. Expect: no badge, existing layout unchanged.
- Load `/admin/payments/:id` for a completed payment with `programAssigned=false` AND no queue entry (harder to synthesize — check any historical genuine failure). Expect: original amber "Program Not Assigned" + Retry/Mark Done unchanged.

**Commit + push:**
```
cd pft-dashboard
git branch --show-current    # MUST be main-2026
git add src/app/\(dashboard\)/_components/modules/admin/payments-history/PaymentsTable.tsx \
        src/app/\(dashboard\)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx
# also add the _shared/paymentQueueLabel.ts if you created it
git commit -m "fix(09-01): render fundedDeferral queue state on admin payment views

Admin PaymentsTable ProgramAssignmentBadge and PaymentDetailsContainer
Action Required card now branch on payment.fundedDeferral (populated
server-side by getPaymentHistoryAdmin in Phase 9 Task 1):

- pending/processing queue entry present  → 'Awaiting KYC' /
  'Awaiting Contract' / 'Awaiting KYC & Contract' / 'In Funded Queue'
  informational badge, Retry + Mark Done HIDDEN
- no queue entry → existing amber 'Program Not Assigned' + Retry +
  Mark Done unchanged (genuine system failure path preserved)

Shared getQueueStateLabel helper mirrors user-facing getFundedHoldLabel
(hardcoded EN — admin components do not use i18n per repo convention).

Ticket: cmqbzq6vc007ds50k008tr3du (DEV — PAP funded queue gate)"
git push origin main-2026
```
  </action>
  <verify>
- `cd pft-dashboard && git branch --show-current` prints `main-2026`.
- `grep -n "getQueueStateLabel" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/payments-history/PaymentsTable.tsx` shows definition + usage.
- `grep -n "getQueueStateLabel" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx` shows usage.
- `grep -n "Awaiting KYC\\|Awaiting Contract\\|In Funded Queue" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/` returns the four label strings across the two admin files.
- `grep -n "fundedDeferral" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/` returns hits in BOTH files.
- `grep -c "Program Not Assigned" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/payments-history/PaymentsTable.tsx` → still ≥1 (fallback path preserved).
- `grep -c "Program Not Assigned\\|Action Required" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/payment-details/PaymentDetailsContainer.tsx` → still ≥1 (fallback path preserved).
- `git diff HEAD~1 -- pft-dashboard/src/hooks/usePayments.ts` → EMPTY (type not changed).
- `git diff HEAD~1 -- pft-dashboard/src/app/\\(dashboard\\)/_components/modules/users/payment-history/PaymentHistoryContainer.tsx` → EMPTY (user-facing untouched).
- Scoped `tsc --noEmit --skipLibCheck` on the two admin files exits 0 (or only shows unrelated pre-existing errors).
- `git log -1 --format=%s origin/main-2026` shows `fix(09-01): render fundedDeferral queue state on admin payment views`.
  </verify>
  <done>
- `ProgramAssignmentBadge` in `PaymentsTable.tsx` renders `queueLabel` badge (blue, informational, no buttons) when `payment.fundedDeferral` has status pending/processing; renders EXISTING amber warning + buttons otherwise.
- `PaymentDetailsContainer.tsx` renders a "Funded Account On Hold" informational card (no Retry/Mark Done) when `fundedDeferral` is active; renders EXISTING "Action Required" card with buttons otherwise.
- Shared `getQueueStateLabel(deferral)` helper exists and is exported from one location; both components import from that location.
- Outer guard on `programAssigned === false` is preserved unchanged in both components.
- `PaymentData` type is unchanged (`fundedDeferral` field was already declared).
- User-facing `PaymentHistoryContainer` is unchanged.
- Commit pushed to `origin/main-2026`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Post-deploy human-verify against live NSF diagnostic case (DEFERRED — per 04.1 / 05-01 / 06-01 / 07-02 / 08-01 convention)</name>
  <what-built>
Backend attaches `fundedDeferral` to every admin payment row via a `paymentId`-keyed FundedProgressionQueue batch join + sparse index. Admin PaymentsTable and PaymentDetailsContainer branch on `fundedDeferral` to render "Awaiting KYC" / "Awaiting Contract" / "Awaiting KYC & Contract" / "In Funded Queue" informational badges and hide Retry + Mark Done, while preserving the amber "Program Not Assigned" + buttons for genuine failures.

Once main-2026 deploys (dashboard + backend), the diagnostic case in NSF is:
- User `misabih1989@gmail.com` (userId `6a249e9b9886435ab02e710e`)
- Payment `6a2c08b1ab4caef5631099a2` (payAfterPassRemainingPayment=true, programAssigned=true — misleading)
- Queue entry `6a2c0a5615385fe987c37953` (status=pending, kycApproved=false, contractApproved=true → expected label: "Awaiting KYC")
  </what-built>
  <how-to-verify>
**This task is DEFERRED until after main-2026 deploys to pft-dashboard + the NSF backend.** Do NOT block on it during plan execution.

When the deploy lands:

1. **Sanity — backend response shape (curl / DevTools Network tab):**
   - As an admin on the NSF (nextstagefunded) dashboard, open `/admin/payments` and search for the diagnostic user.
   - In DevTools → Network, inspect the `GET /api/payments/history-admin` XHR response for the page containing payment `6a2c08b1ab4caef5631099a2`.
   - Confirm the row includes:
     ```json
     "fundedDeferral": {
       "reason": "kyc_pending" | "both_pending" | "manual_approval_pending",
       "status": "pending",
       "kycApproved": false,
       "contractApproved": true
     }
     ```
   - If cache stale, append `?noCache=1` to the request URL (or wait 30s — server TTL).

2. **Table — real queue label + no buttons on the diagnostic row:**
   - The diagnostic payment row shows a BLUE "Awaiting KYC" badge (kycApproved=false wins over contractApproved=true per label rules).
   - Retry and Mark Done buttons are NOT rendered on this row.

3. **Details view — informational card:**
   - Click into payment `6a2c08b1ab4caef5631099a2`.
   - The "Funded Account On Hold" informational card renders instead of the "Action Required: Program Not Assigned" card.
   - Body text mentions "waiting on KYC verification" and "no manual action required".
   - No Retry or Mark Done buttons on this card.

4. **Regression — fallback path preserved (genuine failure):**
   - Find (or synthesize by picking any historical) a PAP payment row with `programAssigned=false` AND no matching queue entry (e.g. an old payment where enqueue itself failed). If none in NSF, spot-check on another brand.
   - Confirm the AMBER "Program Not Assigned" badge + Retry + Mark Done buttons still render exactly as before.
   - Click Retry — confirm the existing mutation still fires and returns 200 (behavior unchanged).

5. **Regression — non-PAP rows unaffected:**
   - Pick any non-PAP completed payment (`payAfterPass=false`).
   - Confirm no badge is shown; layout is exactly as before.

6. **Regression — user-facing history untouched:**
   - Log in AS the diagnostic user (or use another compliance-held PAP user).
   - Open `/payment-history` (client view).
   - Confirm the existing user-facing funded-hold label still renders (`t("paymentHistory.fundedHoldKyc")` etc.) — should be unchanged from pre-deploy.

7. **Sparse index — sanity check on backend host (optional, DBA-friendly):**
   - Run against the NSF DB: `db.fundedprogressionqueues.getIndexes()` — expect a `{ paymentId: 1 }` sparse index alongside the existing indexes.
   - Confirm no `getPaymentHistoryAdmin` slow-query logs after deploy.

8. **Ticket closure:**
   - Reply on DEV ticket `cmqbzq6vc007ds50k008tr3du`: "Admin payment view now surfaces real queue state (Awaiting KYC / Awaiting Contract / In Funded Queue) for PAP funded-leg rows with pending queue entries; Retry + Mark Done hidden on those rows; existing Program Not Assigned + buttons preserved for genuine failures. Verified against NSF payment 6a2c08b1ab4caef5631099a2." Set status WAITING_CLIENT per MEMORY.md `feedback_ticket_status_workflow.md`.

**If verification fails at ANY step:**
- Snapshot the failing screen + network response.
- Do NOT flip labels back on production data — the fallback path preserves the old UI for any row without `fundedDeferral`.
- Open a gap-closure phase (`/gsd:plan-phase 09 --gaps`) with a `-VERIFICATION.md` capturing the observed behavior vs expected.
  </how-to-verify>
  <resume-signal>
After the deploy verification steps above pass (backend attaches fundedDeferral + admin UI renders the real queue label + fallback path unchanged), mark this task done and update the roadmap. Record the deploy timestamp and NSF diagnostic payment screenshot reference in the SUMMARY.

If deploy is delayed and this remains open, leave the plan in "code complete, awaiting deploy" state (matches phases 04.1 / 05 / 06 / 07 / 08 convention).
  </resume-signal>
</task>

</tasks>

<verification>
- `grep -n "paymentIdToDeferral" pft-backend/src/app/modules/Payment/payment.service.modular.ts | wc -l` → ≥2 (user-facing + new admin).
- `grep -n "fundedDeferral" pft-backend/src/app/modules/Payment/payment.service.modular.ts | wc -l` → ≥2.
- `grep -n "paymentId: 1" pft-backend/src/app/modules/FundedProgressionQueue/fundedProgressionQueue.model.ts` → new sparse index present.
- `grep -rn "getQueueStateLabel" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/` → definition + 2 usages.
- `grep -rn "fundedDeferral" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/` → present in both admin files.
- `grep -c "Program Not Assigned" pft-dashboard/src/app/\\(dashboard\\)/_components/modules/admin/payments-history/PaymentsTable.tsx` → still ≥1 (fallback preserved).
- `git log --oneline origin/main-2026 -5` (pft-backend) → shows `fix(09-01): attach fundedDeferral to admin payments + index queue.paymentId`.
- `git log --oneline origin/main-2026 -5` (pft-dashboard) → shows `fix(09-01): render fundedDeferral queue state on admin payment views`.
- No changes in `pft-dashboard/src/hooks/usePayments.ts` (type already had fundedDeferral).
- No changes in `pft-dashboard/src/app/(dashboard)/_components/modules/users/payment-history/PaymentHistoryContainer.tsx` (user-facing untouched).
- No changes in pft-rule-checker, super-admin, or any brand-backend-only branch.
- Post-deploy verify (Task 3) DEFERRED — record status in SUMMARY.
</verification>

<success_criteria>
- One backend commit + one dashboard commit pushed to `origin/main-2026`.
- Admin `getPaymentHistoryAdmin` response now carries `fundedDeferral` per row (populated by `paymentId`-keyed batch join with sort-desc + first-wins).
- `FundedProgressionQueue` has a sparse `{ paymentId: 1 }` schema index.
- Admin PaymentsTable + PaymentDetailsContainer render the real queue-state label ("Awaiting KYC" / "Awaiting Contract" / "Awaiting KYC & Contract" / "In Funded Queue") and hide Retry + Mark Done when `fundedDeferral` is present with status ∈ {pending, processing}.
- Rows with NO `fundedDeferral` OR with terminal status (completed/failed/rejected) render the EXISTING "Program Not Assigned" amber badge + Retry + Mark Done buttons unchanged.
- Non-PAP rows and rows with `programAssigned=true` (the outer guard fails) are unaffected — outer guard preserved.
- `PaymentData` type unchanged (field already declared).
- User-facing `PaymentHistoryContainer` unchanged.
- No changes to rule-checker, super-admin, or brand-backend-only branches.
- Task 3 deferred to post-deploy ops verification (matches recent-phase convention).
- OUT OF SCOPE (explicit): PAP-02 (Retry button relabel) + PAP-03 (queue reason staleness). Do NOT touch either in this plan.
</success_criteria>

<output>
After code completion (Tasks 1 + 2 done, Task 3 deferred), create `.planning/phases/09-pap-funded-queue-state-label/09-01-SUMMARY.md` covering:

- Backend change: exact insertion point in `getPaymentHistoryAdmin` (line reference + before/after), the sparse index addition, the skipEnrichment guard resolution.
- Dashboard change: where `getQueueStateLabel` lives (inline in PaymentsTable OR extracted to `_shared/paymentQueueLabel.ts` — record the pick + reason), the JSX branches added to `ProgramAssignmentBadge` + `PaymentDetailsContainer`, list of icons imported.
- Confirm the 4 files not touched: `usePayments.ts` (type already had field), user-facing `PaymentHistoryContainer.tsx`, rule-checker, super-admin.
- Commit SHAs pushed to `origin/main-2026` (both repos).
- Diagnostic case pointer for Task 3: NSF payment 6a2c08b1ab4caef5631099a2 / queue 6a2c0a5615385fe987c37953 / user misabih1989@gmail.com — expected label "Awaiting KYC".
- Post-deploy verification status: DEFERRED with checklist mirroring Task 3.
- Explicit note: PAP-02 + PAP-03 remain deferred to v1.3 (not addressed here).
- Cross-link: ROADMAP.md Phase 9 entry, ticket cmqbzq6vc007ds50k008tr3du (DEV), MEMORY.md `reference_pap_funded_queue_gate.md`.
</output>
