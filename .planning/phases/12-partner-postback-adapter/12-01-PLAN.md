---
phase: 12-partner-postback-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-backend/src/app/modules/Tracking/tracking.interface.ts
  - pft-backend/src/app/modules/Tracking/tracking.constants.ts
  - pft-backend/src/app/modules/Tracking/tracking.model.ts
  - pft-backend/src/app/modules/Tracking/tracking.validation.ts
autonomous: true

must_haves:
  truths:
    - "The DestinationName type includes \"partnerPostback\" so the adapter and matrix can reference it"
    - "A per-brand TrackingSettings.destinations.partnerPostback config exists with enabled toggle + registrationUrl + conversionUrl, defaulting to disabled + empty URLs"
    - "PUT /api/tracking/settings accepts and persists partnerPostback.registrationUrl / conversionUrl / enabled (validation does NOT strip them)"
    - "The DEFAULT_EVENT_TOGGLES matrix has a partnerPostback column, true ONLY for signup_completed, purchase_completed, pap_payment_completed; false for every other event (incl. free_*_signup)"
    - "No brand fires partnerPostback unless an admin explicitly enables it and sets a URL (brand isolation via enabled=false default)"
  artifacts:
    - path: "pft-backend/src/app/modules/Tracking/tracking.interface.ts"
      provides: "\"partnerPostback\" in DESTINATIONS tuple + IPartnerPostbackConfig + partnerPostback field on ITrackingSettings.destinations"
      contains: "partnerPostback"
    - path: "pft-backend/src/app/modules/Tracking/tracking.constants.ts"
      provides: "partnerPostback column on the DEFAULT_EVENT_TOGGLES row type + every row + defaultEventsForDestination support"
      contains: "partnerPostback"
    - path: "pft-backend/src/app/modules/Tracking/tracking.model.ts"
      provides: "PartnerPostbackConfigSchema (disabled + empty-string URL defaults) registered under destinations"
      contains: "partnerPostback"
    - path: "pft-backend/src/app/modules/Tracking/tracking.validation.ts"
      provides: "partnerPostback block in upsertTrackingSettingsValidation so PUT accepts it"
      contains: "partnerPostback"
  key_links:
    - from: "tracking.interface.ts DESTINATIONS tuple"
      to: "DestinationName type"
      via: "typeof DESTINATIONS[number]"
      pattern: "partnerPostback"
    - from: "tracking.constants.ts DEFAULT_EVENT_TOGGLES row type"
      to: "every event row"
      via: "TS required field forces every row to declare partnerPostback (compile-time completeness)"
      pattern: "partnerPostback:\\s*(true|false)"
    - from: "tracking.model.ts destinations block"
      to: "PartnerPostbackConfigSchema"
      via: "mongoose sub-schema default () => ({})"
      pattern: "partnerPostback:\\s*\\{\\s*type: PartnerPostbackConfigSchema"
---

<objective>
Add the `partnerPostback` destination as a first-class config surface (CRM-09): a new DestinationName, a per-brand `TrackingSettings.destinations.partnerPostback` config shape (enabled toggle + registrationUrl + conversionUrl), its mongoose sub-schema with disabled+empty defaults, the PUT-settings validation block, and the `partnerPostback` column in the event→destination matrix (true only for the three conversion-eligible events).

Purpose: The framework dispatcher (`tracking.service.ts` dispatch/dedup/log/gating) is already built and generic — it will route to `partnerPostback` the moment the destination NAME, the per-event matrix column, and the per-brand config type exist. This plan creates exactly those config-layer artifacts so plan 12-02's adapter has a typed contract to implement and the dispatcher's `destinationAcceptsEvent()` gate + admin `PUT /api/tracking/settings` both recognize the new destination. Nothing partner-specific is hardcoded; Trading Cult's real URL templates are set at config time via the existing PUT endpoint after deploy.

Output: `partnerPostback` in DESTINATIONS; `IPartnerPostbackConfig`; `ITrackingSettings.destinations.partnerPostback`; `PartnerPostbackConfigSchema`; validation block; matrix column.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-partner-postback-adapter/12-RESEARCH.md

# The four files this plan edits (read before editing — line anchors below are from HEAD 982ba9a1 = origin/main-2026, may drift by a few lines):
@pft-backend/src/app/modules/Tracking/tracking.interface.ts
@pft-backend/src/app/modules/Tracking/tracking.constants.ts
@pft-backend/src/app/modules/Tracking/tracking.model.ts
@pft-backend/src/app/modules/Tracking/tracking.validation.ts
</context>

<preflight>
Code lives in the NESTED git repo `pft-backend/` (its own remote, branch `main-2026`). Planning docs live in the PARENT repo (`PFT-WhiteLabel-v2-staging`, branch `main`). This plan edits CODE only.

Before editing, from inside `pft-backend/`:
1. `cd pft-backend && git branch --show-current` → MUST be `main-2026`. If not, STOP.
2. `git fetch origin main-2026 && git log origin/main-2026 --oneline -3` — confirm HEAD is `982ba9a1` (or note newer). If a newer commit already added `partnerPostback` config, defer to remote (rebase, do not re-add) per the defer-to-remote convention.
3. All four target files are in the SAME module dir; scoped typecheck at the end covers them together. Backend full tsc OOMs (exit 134) — NEVER run project-wide tsc.
</preflight>

<tasks>

<task type="auto">
  <name>Task 1: Add partnerPostback to DESTINATIONS + IPartnerPostbackConfig + ITrackingSettings.destinations</name>
  <files>pft-backend/src/app/modules/Tracking/tracking.interface.ts</files>
  <action>
Three edits in this one file:

1. DESTINATIONS tuple (`~:51-59`, currently ends `"intercom",\n] as const;`): add `"partnerPostback"` as the last element BEFORE `] as const;`. `DestinationName = (typeof DESTINATIONS)[number]` (`~:61`) then includes it automatically — no separate edit.

2. New interface, place it right after `IConversionWebhookConfig` (`~:116-119`, which is `{ webhookUrl: string; webhookSecret: string }`). Mirror that shape:
```ts
export interface IPartnerPostbackConfig extends IDestinationToggle {
  /** GET URL template fired on registration (goal=registration). Supports {clickid}/{goal}/{payout}/{currency} macros. Empty = disabled. */
  registrationUrl: string;
  /** GET URL template fired on the user's FIRST completed purchase (goal=conversion). Supports the same macros. Empty = disabled. */
  conversionUrl: string;
}
```
`IDestinationToggle` (`~:65`) already supplies `enabled` + `useGtm` + `events` — do NOT redeclare them.

3. `ITrackingSettings.destinations` object (`~:145-153`): add `partnerPostback: IPartnerPostbackConfig;` as the last property inside the `destinations: { ... }` block (after `intercom: IIntercomConfig;`).

Do NOT touch ITrackingEventPayload, EVENT_NAMES, or the event-log types.
  </action>
  <verify>
`cd pft-backend && grep -n "partnerPostback" src/app/modules/Tracking/tracking.interface.ts` shows exactly 3 hits (tuple, interface name, destinations field). `grep -n "IPartnerPostbackConfig" src/app/modules/Tracking/tracking.interface.ts` shows 2 (definition + destinations field type).
  </verify>
  <done>DESTINATIONS includes "partnerPostback"; IPartnerPostbackConfig exists mirroring IConversionWebhookConfig but with registrationUrl/conversionUrl; ITrackingSettings.destinations.partnerPostback is typed IPartnerPostbackConfig.</done>
</task>

<task type="auto">
  <name>Task 2: Add partnerPostback column to the DEFAULT_EVENT_TOGGLES matrix (type + every row)</name>
  <files>pft-backend/src/app/modules/Tracking/tracking.constants.ts</files>
  <action>
`DEFAULT_EVENT_TOGGLES` (`~:12-73`) is `Record<EventName, { metaCapi: boolean; googleAds: boolean; ga4: boolean; klaviyo: boolean; conversionWebhook: boolean; getResponse: boolean; intercom: boolean }>`. This inline row-object type IS the "DestinationToggleRow" the research refers to.

1. Add `partnerPostback: boolean;` to that inline row type (`~:14-22`, inside the `Record<EventName, {...}>` object type, e.g. after `intercom: boolean;`). This makes TS require the field on EVERY row → compile-time completeness (open item 2).

2. Add `partnerPostback: <value>` to EVERY event row (`~:27-72`). Set `partnerPostback: true` ONLY on these three rows:
   - `signup_completed` (`~:29`)
   - `purchase_completed` (`~:40`)
   - `pap_payment_completed` (`~:59`)
   All OTHER rows (including `free_trial_signup` :44, `free_challenge_signup` :45, `pap_free_signup` :46, and every KYC/lifecycle/payout/re-engagement row) get `partnerPostback: false`. This enforces the locked "$0/free = registration postback only, no $0 conversion" decision: free users still fire `signup_completed` (→ registration) but never a conversion-eligible event with real value.

Formatting: match the existing aligned-object style on each row (append the new key at the end of each row object). `defaultEventsForDestination()` (`~:78`) consumes the table generically via `DEFAULT_EVENT_TOGGLES[name][destination]` — no change needed there; adding the column is sufficient for the model to seed a per-event map for partnerPostback.

Do NOT touch EVENT_MAPPING (`~:91+`) — the partner postback does not need a GA4/FB/Klaviyo name mapping.
  </action>
  <verify>
`cd pft-backend && grep -c "partnerPostback: true" src/app/modules/Tracking/tracking.constants.ts` returns exactly 3. `grep -c "partnerPostback:" src/app/modules/Tracking/tracking.constants.ts` returns 34 (1 type decl + 33 event rows). `npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.constants.ts src/app/modules/Tracking/tracking.interface.ts` reports 0 errors (missing rows would be TS2741 "property partnerPostback is missing").
  </verify>
  <done>Row type requires partnerPostback; all 33 rows declare it; exactly signup_completed + purchase_completed + pap_payment_completed are true; scoped tsc clean (proving no row was missed).</done>
</task>

<task type="auto">
  <name>Task 3: Add PartnerPostbackConfigSchema to the model + the PUT-settings validation block</name>
  <files>pft-backend/src/app/modules/Tracking/tracking.model.ts, pft-backend/src/app/modules/Tracking/tracking.validation.ts</files>
  <action>
MODEL (`tracking.model.ts`):
1. Import: add `type IPartnerPostbackConfig,` to the existing type import from `./tracking.interface` (`~:5-15`, alongside `IConversionWebhookConfig`).
2. Define the sub-schema next to `ConversionWebhookConfigSchema` (`~:74-80`), using the same `destinationBase<T>()` helper (`~:24`) — this is what supplies the disabled+empty defaults (`enabled: {default:false}` at `~:30`; the extra fields default to `""`):
```ts
const PartnerPostbackConfigSchema = destinationBase<IPartnerPostbackConfig>(
  "partnerPostback",
  {
    registrationUrl: { type: String, default: "" },
    conversionUrl: { type: String, default: "" },
  },
);
```
3. Register under `destinations` in `TrackingSettingsSchema` (`~:117-128`, after `intercom`):
```ts
      partnerPostback: { type: PartnerPostbackConfigSchema, default: () => ({}) },
```
No toJSON masking entry needed (`~:141-151`) — partnerPostback has no secret field (URLs are not credentials). Leave the mask block unchanged.

VALIDATION (`tracking.validation.ts`):
4. In `upsertTrackingSettingsValidation.body.destinations` (`~:26-85`), add a `partnerPostback` block after `intercom` (`~:78-83`), mirroring the `conversionWebhook` block (`~:66-71`):
```ts
        partnerPostback: destinationToggleSchema
          .extend({
            registrationUrl: z.string().url().optional().or(z.literal("")),
            conversionUrl: z.string().url().optional().or(z.literal("")),
          })
          .optional(),
```
This is ESSENTIAL for CRM-09: zod's default object parsing STRIPS unknown keys, so without this block a `PUT /api/tracking/settings` carrying `destinations.partnerPostback.*` would be silently dropped before the controller's `flattenForSet` ever sees it → the config could never be set. The `.url().or(z.literal(""))` mirror allows a real URL template or empty string (disabled), matching conversionWebhook. `destinationToggleSchema` already permits `enabled`.

No controller change: `flattenForSet` (controller `~:272`) recurses plain objects generically and will emit `destinations.partnerPostback.registrationUrl` dotted `$set` paths automatically, so a partial PATCH merges correctly.
  </action>
  <verify>
`cd pft-backend && grep -n "PartnerPostbackConfigSchema" src/app/modules/Tracking/tracking.model.ts` shows 2 (definition + destinations registration). `grep -n "partnerPostback" src/app/modules/Tracking/tracking.validation.ts` shows 1 (the block). Scoped tsc: `npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.model.ts src/app/modules/Tracking/tracking.validation.ts src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.constants.ts` → 0 errors.
  </verify>
  <done>PartnerPostbackConfigSchema defined + registered under destinations with disabled+empty-URL defaults; validation accepts partnerPostback.registrationUrl/conversionUrl/enabled; scoped tsc across all four config files clean.</done>
</task>

</tasks>

<verification>
- `cd pft-backend && npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.constants.ts src/app/modules/Tracking/tracking.model.ts src/app/modules/Tracking/tracking.validation.ts` → 0 errors (NEVER project-wide tsc — OOMs at 134).
- `grep -c "partnerPostback: true" src/app/modules/Tracking/tracking.constants.ts` → 3.
- DESTINATIONS tuple, IPartnerPostbackConfig, ITrackingSettings.destinations.partnerPostback, PartnerPostbackConfigSchema, and the validation block all present.
- Defaults prove brand isolation: a fresh settings doc has `destinations.partnerPostback.enabled === false` and both URLs `""` → no brand fires until configured.
</verification>

<success_criteria>
- CRM-09 config surface complete: `partnerPostback` is a DestinationName; per-brand config type + mongoose schema + PUT validation exist; disabled + empty-URL by default.
- Matrix routes exactly the three conversion-eligible events to partnerPostback; free/$0 events do not.
- Nothing Trading-Cult-specific is hardcoded (real URLs set post-deploy via PUT /api/tracking/settings).
- Scoped typecheck clean across all four files.
</success_criteria>

<commit>
Commit CODE to the pft-backend `main-2026` repo (NOT the parent .planning repo):
```
cd pft-backend
git add src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.constants.ts src/app/modules/Tracking/tracking.model.ts src/app/modules/Tracking/tracking.validation.ts
git commit -m "feat(12-01): partnerPostback destination config — DESTINATIONS + IPartnerPostbackConfig + schema + PUT validation + matrix column (CRM-09)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
git push origin main-2026
```
</commit>

<output>
After completion, create `.planning/phases/12-partner-postback-adapter/12-01-SUMMARY.md` (in the PARENT repo). Record: exact final line numbers touched, the commit SHA on main-2026, confirmation that partnerPostback defaults to disabled+empty, and the DestinationName + IPartnerPostbackConfig contract that plan 12-02 consumes.
</output>
