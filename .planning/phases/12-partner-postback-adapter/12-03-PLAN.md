---
phase: 12-partner-postback-adapter
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "The adapter is proven (with a stub fetch + fake settings) to: skip when partnerClickId absent, skip when url empty, skip a conversion when isFirstPurchase !== true, GET registrationUrl w/ goal=registration + encoded clickid for signup_completed, GET conversionUrl w/ goal=conversion + payout=usdAmount + currency=USD for a first purchase"
    - "A partnerClickId with URL-special chars (+, =, /) reaches the partner exactly (encodeURIComponent round-trips to the original)"
    - "The full config→dispatch→matrix→adapter chain typechecks together (no missing wiring)"
    - "The post-deploy live Trading Cult postback test is captured as a DEFERRED human-verify checkpoint (matches every prior v1.3 phase)"
  artifacts:
    - path: ".planning/phases/12-partner-postback-adapter/12-03-VERIFY.md"
      provides: "Recorded behavioral-verification results (skip-guard cases, encoding round-trip, FTD gate) + the deferred live-test checklist"
      contains: "encodeURIComponent"
  key_links:
    - from: "verification harness"
      to: "partnerPostbackAdapter.send()"
      via: "direct call with stubbed global.fetch + fake ctx.settings"
      pattern: "partnerPostbackAdapter"
---

<objective>
Prove the Phase 12 success criteria are met at the code level (SC1-4) and record the DEFERRED post-deploy live checkpoint (SC5). Because there is no live partner endpoint available pre-deploy, verification exercises `partnerPostbackAdapter.send()` directly with a stubbed `global.fetch` and a fake `ctx.settings` — asserting the requested URL, method, goal, payout, and skip decisions for each case. Then a human-verify checkpoint records the live Trading Cult test to run after the next `main-2026` deploy.

Purpose: Closes CRM-07/CRM-09 verification. The behavioral harness is the executable proof that skip-guards + FTD gate + encode-once work exactly as specified before any live traffic. The deferred checkpoint honors the project convention (post-deploy live human-verify is not a phase blocker).

Output: `12-03-VERIFY.md` with recorded results + deferred live checklist. Milestone v1.3 code-complete.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-partner-postback-adapter/12-RESEARCH.md
@.planning/phases/12-partner-postback-adapter/12-01-SUMMARY.md
@.planning/phases/12-partner-postback-adapter/12-02-SUMMARY.md
@pft-backend/src/app/modules/Tracking/destinations/partner-postback.ts
</context>

<preflight>
Requires 12-02 merged (the adapter exists + is registered). The harness is a THROWAWAY script — do not commit it into the app; run it, capture output into 12-03-VERIFY.md, then delete it. If the project has an existing test runner (check `pft-backend/package.json` scripts for `test`/`jest`/`vitest`), a temporary spec under the existing test dir is fine but still remove it after — Phase 12 ships no permanent test per prior-phase convention.
</preflight>

<tasks>

<task type="auto">
  <name>Task 1: Behavioral verification of partnerPostbackAdapter.send() via stubbed fetch</name>
  <files>pft-backend/scripts/_tmp_verify_partner_postback.ts (throwaway — delete after)</files>
  <action>
Write a throwaway script (ts-node or compile-and-run, whichever the repo already uses — check package.json) that imports `partnerPostbackAdapter` from `../src/app/modules/Tracking/destinations/partner-postback` and calls `.send(payload, ctx)` directly with:
- `global.fetch` stubbed to a recorder: capture the `url` + `method` of each call and return `{ ok: true, status: 200 }` (and, for one case, return `{ ok: false, status: 503 }` first then `{ ok:true, status:200 }` to prove the single 5xx retry fires twice).
- `ctx = { req: null, settings: { destinations: { partnerPostback: { enabled: true, registrationUrl: "https://partner.test/reg?clickid={clickid}&goal={goal}", conversionUrl: "https://partner.test/conv?clickid={clickid}&goal={goal}&payout={payout}&currency={currency}" } } } }` (cast as any — only the partnerPostback field matters).

Assert (print PASS/FAIL for each; exit non-zero on any FAIL):
1. partnerClickId absent → `{status:"skipped"}`, fetch NOT called.
2. enabled:false → `{status:"skipped"}`, fetch NOT called.
3. empty registrationUrl ("") for signup_completed → `{status:"skipped"}`, fetch NOT called.
4. signup_completed + clickid "ABC123" → fetch called ONCE, url === `https://partner.test/reg?clickid=ABC123&goal=registration`, method GET, result "sent".
5. purchase_completed + clickid + isFirstPurchase:false → `{status:"skipped"}` (FTD gate), fetch NOT called.
6. purchase_completed + clickid + isFirstPurchase:true + value:99 → fetch ONCE, url === `https://partner.test/conv?clickid=<enc>&goal=conversion&payout=99&currency=USD`, result "sent".
7. pap_payment_completed + clickid + isFirstPurchase:true + value:150 → same conversion shape, payout=150.
8. ENCODING: clickid `"a+b=c/d"` on signup → url contains `clickid=a%2Bb%3Dc%2Fd` and `decodeURIComponent("a%2Bb%3Dc%2Fd") === "a+b=c/d"` (round-trips to original — SC4).
9. RETRY: conversion first fetch returns 503, stub second returns 200 → fetch called TWICE, result "sent".
10. FAILURE: fetch throws (simulate timeout) → `{status:"failed"}`, no throw escapes.

Run it, capture stdout. Then DELETE the script: `rm pft-backend/scripts/_tmp_verify_partner_postback.ts` (and any temp compiled output).
  </action>
  <verify>Script prints PASS for all 10 cases and exits 0. If any FAIL, fix the adapter in 12-02's file (this indicates a real bug) and re-run. Confirm the throwaway script is deleted: `cd pft-backend && ls scripts/_tmp_verify_partner_postback.ts 2>&1` → "No such file".</verify>
  <done>All 10 behavioral assertions pass (skip-guards, FTD gate, registration GET, conversion GET w/ usdAmount payout + USD currency, encode-once round-trip, 5xx single retry, failed-not-thrown); throwaway script removed.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-chain typecheck + record results in 12-03-VERIFY.md</name>
  <files>.planning/phases/12-partner-postback-adapter/12-03-VERIFY.md</files>
  <action>
1. Run the scoped whole-chain typecheck to prove nothing is dangling:
`cd pft-backend && npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.constants.ts src/app/modules/Tracking/tracking.model.ts src/app/modules/Tracking/tracking.validation.ts src/app/modules/Tracking/tracking.service.ts src/app/modules/Tracking/destinations/index.ts src/app/modules/Tracking/destinations/partner-postback.ts` → expect 0 errors. (NEVER project-wide — OOMs 134.)

2. Create `12-03-VERIFY.md` (PARENT repo) recording:
   - The 10 behavioral PASS results from Task 1 (paste the captured output).
   - The whole-chain tsc result (0 errors).
   - Mapping table: SC1 (adapter registered + skip on absent clickid/empty url) → cases 1-4; SC2 (registration GET) → case 4; SC3 (conversion GET + FTD gate + payout=usdAmount) → cases 5-7; SC4 (URL-special-char encode) → case 8; CRM-09 config disabled-by-default → 12-01 schema defaults.
   - The DEFERRED live checklist (SC5), verbatim below, to run post-deploy.
  </action>
  <verify>`12-03-VERIFY.md` exists and contains the 10 PASS lines, the tsc-0-errors line, the SC→case mapping, and the deferred live checklist. Whole-chain tsc reported 0 errors.</verify>
  <done>VERIFY.md records all SC1-4 as met via the harness + whole-chain tsc clean; SC5 captured as deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The `partnerPostback` GET adapter + per-brand config, code-complete on `main-2026` and behavior-verified locally (skip-guards, FTD gate, encode-once, 5xx retry all PASS). This checkpoint is the DEFERRED post-deploy live test against the real Trading Cult partner endpoint (Phase 12 SC5) — it is NOT a blocker for marking Phase 12 code-complete, matching every prior v1.3 phase.
  </what-built>
  <how-to-verify>
Run these AFTER the next `main-2026` deploy reaches the Trading Cult backend (`api-tradingcult.propfirmstech.com`), once the partner's real registration + conversion URL templates are known:

1. Configure (Trading Cult DB only, via existing admin endpoint):
   `PUT /api/tracking/settings` with body:
   `{ "destinations": { "partnerPostback": { "enabled": true, "registrationUrl": "<partner registration template with {clickid}/{goal}>", "conversionUrl": "<partner conversion template with {clickid}/{goal}/{payout}/{currency}>" } } }`
   Confirm 200 and that a follow-up `GET /api/tracking/settings` shows the saved URLs (they are NOT masked — they are not secrets).

2. Registration test: visit a partner tracking link `GET /api/tracking/track?clickid=TESTCLICK123`, complete a registration (one-step or OTP). Expect: a `TrackingEventLog` row for `destination:"partnerPostback"`, `eventName:"signup_completed"`, `status:"sent"`; and the partner's dashboard shows the registration with clickid `TESTCLICK123`.

3. Conversion test: same user completes their FIRST purchase (standard challenge OR PAP funded-leg). Expect: a `partnerPostback` log row for `purchase_completed`/`pap_payment_completed`, `status:"sent"`, and the partner sees goal=conversion with `payout` = the USD amount (NOT a JPY figure). A second purchase by the same user must NOT produce a conversion `partnerPostback` row (FTD gate).

4. Brand isolation: confirm a non-Trading-Cult brand (partnerPostback still disabled) fires ZERO partnerPostback log rows.
  </how-to-verify>
  <resume-signal>Type "approved" once the live registration + conversion postbacks land in the partner system (or describe the failure). Deferred — do not block Phase 12 completion on this.</resume-signal>
</task>

</tasks>

<verification>
- Behavioral harness: 10/10 PASS (skip-guards, FTD gate, registration + conversion GET shapes, encode-once round-trip, 5xx retry, failed-not-thrown).
- Whole-chain scoped tsc: 0 errors across interface + constants + model + validation + service + destinations/index + partner-postback.
- 12-03-VERIFY.md records results + the deferred live checklist.
</verification>

<success_criteria>
- Phase 12 SC1-4 proven at code level; SC5 captured as a deferred post-deploy checkpoint.
- No permanent test committed (throwaway harness deleted per prior-phase convention).
- Milestone v1.3 CRM Partner Tracking is code-complete.
</success_criteria>

<commit>
This plan commits ONLY the planning doc `12-03-VERIFY.md` to the PARENT repo (`.planning/`), gated on `commit_docs=true`. No pft-backend code change (unless the harness surfaced an adapter bug — if so, that fix commits to `main-2026` separately with a `fix(12-02):` message). Do NOT commit the throwaway harness.
```
git add .planning/phases/12-partner-postback-adapter/12-03-VERIFY.md
git commit -m "docs(12-03): partnerPostback behavioral verification + deferred live checklist"
```
</commit>

<output>
After completion, create `.planning/phases/12-partner-postback-adapter/12-03-SUMMARY.md` (PARENT repo). Record: the 10 PASS results, whole-chain tsc clean, that SC5 is deferred to post-deploy, and that milestone v1.3 is code-complete (ready for /gsd:complete-milestone).
</output>
