---
phase: 05-daily-profit-display-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-dashboard/src/hooks/useTradingDashboardData.ts
  - pft-dashboard/src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts
autonomous: true

must_haves:
  truths:
    - "When a CLOSE deal (Entry='1') exists in the buffer with no matching OPEN deal, mergedFromDeals emits a closed-position row carrying that close deal's Profit, not silently dropping it"
    - "For account 13535 on 2026-06-18, the Daily P&L Calendar cell renders $20 (true sum $20.16) — NOT $55 (the buggy matched-only sum $54.85)"
    - "Synthetic orphan-close rows pass isDisplayableMergedTrade (they have DurationMs >= 1 and a valid symbol/positive OpenPrice/non-pending Action)"
    - "Matched OPEN+CLOSE pairing behavior is unchanged for non-orphan deals (existing test fixtures / live data still produce identical row counts and profits for matched pairs)"
    - "A jest/vitest fixture exercises: 1 matched OPEN+CLOSE pair + 1 orphan CLOSE deal, and asserts mergedFromDeals returns 2 closed rows summing to full closed-PnL"
  artifacts:
    - path: "pft-dashboard/src/hooks/useTradingDashboardData.ts"
      provides: "Patched mergedFromDeals with orphan-close emission branch"
      contains: "totalOpen <= 0"
    - path: "pft-dashboard/src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts"
      provides: "Regression test fixture for the orphan-close path"
      contains: "orphan"
  key_links:
    - from: "pft-dashboard/src/hooks/useTradingDashboardData.ts (mergedFromDeals close-deal branch ~line 605)"
      to: "pft-dashboard/src/utils/tradeHistoryDisplay.ts (isDisplayableMergedTrade)"
      via: "emitted row carries DurationMs >= 1, OpenTime == CloseTime, valid Symbol, Action < 2, OpenPrice from PricePosition"
      pattern: "DurationMs.*[>=]\\s*1"
---

<objective>
Fix the dashboard Daily P&L Calendar widget so it reports correct daily profit per account/day instead of dropping orphan close deals.

Purpose: Trader on account 13535 sees +$55 for 2026-06-18 when the true closed P&L for that day is $20.16. Root cause: `mergedFromDeals` in `useTradingDashboardData.ts` does `continue` (silently skips) when a CLOSE deal has no matching OPEN deal in the loaded buffer (common at archive boundary / paginated loads). The orphan -$34.69 loss is dropped: $54.85 = $20.16 − (−$34.69). Ticket cmquy9bqo005pny0kw6j0lr71 (Trading Cult, URGENT).

Output:
- Patched `mergedFromDeals` that emits a synthetic closed-position row for each orphan CLOSE deal.
- Regression test fixture proving the orphan-close path sums correctly.
- SUMMARY.md with commit hash, before/after arithmetic ($54.85 buggy → $20.16 fixed for 2026-06-18 account 13535).

OUT OF SCOPE — DO NOT TOUCH:
- The MT5-vs-DB sync discrepancy: MT5 broker shows $9.90 for 2026-06-18; DB closed-PnL sum is $20.16. That is a separate, smaller data-sync concern. Mention only as a follow-up note in SUMMARY.md.
- `pft-rule-checker` server-side pairing (suspected similar bug). Mention as follow-up DEV ticket suggestion in SUMMARY.md only.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-daily-profit-display-bug/05-RESEARCH.md

# Source file with the bug (the close-deal branch is around line 595-620)
@pft-dashboard/src/hooks/useTradingDashboardData.ts

# Display filter that synthetic rows MUST pass
@pft-dashboard/src/utils/tradeHistoryDisplay.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch mergedFromDeals to emit orphan-close rows + add regression test</name>
  <files>
    pft-dashboard/src/hooks/useTradingDashboardData.ts
    pft-dashboard/src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts
  </files>
  <action>
    1. In `pft-dashboard/src/hooks/useTradingDashboardData.ts`, locate the close-deal branch in `mergedFromDeals` (around line 601-620). The current buggy block is:

    ```typescript
    if (entry === "1") {
      let remain = vol > 0 ? vol : totalOpen;
      if (remain <= 0 || totalOpen <= 0) continue;   // <-- BUG: drops orphan closes
      ...
    }
    ```

    Replace the `if (remain <= 0 || totalOpen <= 0) continue;` line with an orphan-close emission branch. When `totalOpen <= 0` (no open in buffer to match against), emit ONE synthetic closed-position row built from the close deal alone, then `continue`. Keep the matched-pair while-loop below UNCHANGED.

    The synthetic row must carry (use the same field names emitClosed uses elsewhere in this function — read the surrounding code to mirror them exactly):
    - `Symbol` from the close deal
    - `Action` from the close deal (must be < 2 so the display filter accepts it; close deals carry Action 0 or 1 — verify in surrounding logic)
    - `Volume` from the close deal (vol)
    - `Profit` from the close deal (the actual loss/gain — this is the value that was being dropped)
    - `Commission`, `Swap`, `Fee` from the close deal if present (so totals match)
    - `OpenPrice` from the close deal's `PricePosition` field (the close deal carries the position's open price there). Fall back to `(d as any).Price` if PricePosition missing/zero.
    - `ClosePrice` from the close deal's `Price`
    - `OpenTime` = `CloseTime` (we do NOT have the real open time — the open deal is outside the buffer). Both set to the close deal's time (mirror how CloseTime is computed in the matched branch).
    - `CloseTime` from the close deal time
    - `DurationMs: 1` — CRITICAL: must be >= 1 so `isDisplayableMergedTrade` (which requires `durationSec > 0 || durationMs > 0`, see `pft-dashboard/src/utils/tradeHistoryDisplay.ts` line 119) does not drop the row. `durationSec` will be 0 since openTime==closeTime, so `durationMs` MUST carry the truthy signal.
    - `OrderOpen: ""` (unknown), `OrderClose: String(d.Order || "")`
    - `Comment` from the close deal
    - `TP: 0`, `SL: 0` (or pull from comment via slTpFromMt5DealComment if trivial — optional)
    - `Direction`: derive from close deal Action if reasonable, otherwise empty string. Display filter only cares Action < 2; Direction is cosmetic.
    - Any flag like `isOpen: false` (do NOT set isOpen: true — the filter excludes those)
    - Mark this row as orphan-sourced for debuggability: add `_orphanClose: true` (won't affect any consumer, useful for inspection)

    Reference the existing `emitClosed` helper in the same file — the orphan emission should produce a row in the SAME shape, just with synthetic open-side fields.

    Add a brief code comment above the new branch:
    ```
    // Orphan close: the matching open deal is outside the loaded buffer
    // (paginated load / archive boundary). Emit a synthetic closed-position
    // row so daily P&L sums include this profit. DurationMs=1 ensures
    // isDisplayableMergedTrade accepts the row (openTime==closeTime).
    ```

    2. Create `pft-dashboard/src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts` using whichever test runner the repo uses (check `package.json` for vitest vs jest; copy style from any existing test file in pft-dashboard). The fixture:

    - Buffer of 3 deals for one PositionID-pair plus one orphan:
      - Open deal A: PositionID=100, Entry='0', Volume=1, Price=1.1000, Time=T0
      - Close deal A: PositionID=100, Entry='1', Volume=1, Price=1.1010, Profit=10, Time=T1
      - Orphan close B: PositionID=200, Entry='1', Volume=1, PricePosition=1.2000, Price=1.1900, Profit=-34.69, Time=T2 (NO matching open with PositionID=200 in buffer)
    - Call `mergedFromDeals` (export it from the hook file if not already exported, OR test it through a thin wrapper — pick the lowest-friction approach; if neither is feasible add `export` to the function so the test can import it directly).
    - Assertions:
      - Returns exactly 2 closed-position rows
      - Sum of Profit fields === 10 + (-34.69) = -24.69 (use toBeCloseTo for float)
      - The orphan row has DurationMs >= 1
      - The orphan row passes `isDisplayableMergedTrade` (import the filter and call it)
      - The matched row's OpenTime !== CloseTime (existing pairing behavior unchanged)

    3. If `mergedFromDeals` is not currently exported, change it from a const inside the hook to a top-level exported function (or re-export via a `__test__` symbol). Whichever requires the smallest change. Do NOT refactor the hook's structure beyond making the function reachable from a test.

    4. Run the test to confirm it passes.

    5. Also run `npx tsc --noEmit` scoped to the changed file if possible (full tsc OOMs locally per MEMORY.md — do NOT run the full project typecheck).
  </action>
  <verify>
    cd pft-dashboard && (npx vitest run src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts || npx jest src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts) — test PASSES.

    Arithmetic check (manual): simulate the account 13535 / 2026-06-18 case mentally — buffer with 12 close deals where 11 match opens (sum $54.85) and 1 orphan close with Profit=-$34.69. Expected fixed output: 12 rows summing to $20.16. Document this in SUMMARY.md.

    File diff check: only `mergedFromDeals` close-deal branch (entry==='1') changes; the partial-close branch (entry==='2'), the open-deal branch (entry==='0'), and the matched while-loop within entry==='1' are byte-identical to before (except the `continue` line is now an emit-then-continue block).
  </verify>
  <done>
    - `mergedFromDeals` emits a synthetic closed-position row for orphan close deals.
    - New test `mergedFromDeals.orphanClose.test.ts` passes.
    - Test proves orphan-close Profit is included in the merged output and the row passes `isDisplayableMergedTrade`.
    - Matched-pair logic is unchanged (existing behavior preserved).
    - No tsc errors introduced in the modified file.
  </done>
</task>

</tasks>

<verification>
1. The new orphan-close branch is reachable: place a `console.log` temporarily during dev to confirm the branch executes for the test fixture; remove before commit.
2. Arithmetic sanity for ticket cmquy9bqo005pny0kw6j0lr71: with the live account-13535 / 2026-06-18 deal list (12 closes, 1 orphan -$34.69), the Daily P&L Calendar cell would now render +$20 (round of $20.16) instead of +$55 (round of $54.85). The signed value −$34.69 is now SUMMED into the day's total.
3. `isDisplayableMergedTrade` accepts the synthetic row: assertion is in the test itself (call the filter on the orphan row, expect true).
4. Matched-pair flow unchanged: assertion in the test (matched row's OpenTime !== CloseTime; DurationMs > 1ms because openTime != closeTime).
</verification>

<success_criteria>
- File `pft-dashboard/src/hooks/useTradingDashboardData.ts` modified at the close-deal branch only.
- File `pft-dashboard/src/hooks/__tests__/mergedFromDeals.orphanClose.test.ts` created and passing.
- Test covers: (a) matched-pair still works, (b) orphan close emits a row, (c) emitted row passes `isDisplayableMergedTrade`, (d) Profit total = matched + orphan.
- Commit hash recorded in SUMMARY.md along with the $54.85 → $20.16 arithmetic note and the OUT-OF-SCOPE note about $9.90 vs $20.16 MT5/DB sync.
- Follow-up DEV-ticket suggestion noted in SUMMARY.md: verify whether `pft-rule-checker` server-side pairing logic has the same orphan-drop bug.
</success_criteria>

<output>
After completion, create `.planning/phases/05-daily-profit-display-bug/05-01-SUMMARY.md` including:
- Commit hash of the fix
- Before/after arithmetic for account 13535 / 2026-06-18: buggy $54.85 (rendered "+$55") → fixed $20.16 (rendered "+$20")
- Files changed list
- Test name + result
- OUT-OF-SCOPE follow-up: MT5 broker shows $9.90 for 2026-06-18 while DB sum is $20.16 — separate data-sync investigation, do NOT block this fix on it
- OPTIONAL follow-up: open DEV ticket to verify `pft-rule-checker` server-side pairing logic for same orphan-drop bug
- Deploy note: this is pft-dashboard main-2026 — push to main-2026 ships to all brands including Trading Cult. Ticket reply should be "fixed in code, will appear after next dashboard deploy."
</output>
