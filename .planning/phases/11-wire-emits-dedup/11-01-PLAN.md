---
phase: 11-wire-emits-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pft-backend/src/app/modules/Tracking/tracking.interface.ts
  - pft-backend/src/app/modules/Tracking/tracking.events.service.ts
  - pft-backend/src/app/modules/Auth/auth.service.ts
autonomous: true

must_haves:
  truths:
    - "A user who registered after visiting a partner tracking link fires signup_completed carrying their partnerClickId (both one-step and two-step OTP registration paths)"
    - "A user who registered without a partner tracking link fires signup_completed with NO partnerClickId key (skip-when-absent)"
    - "signup_completed can be deduped per-user on a repeat OTP submit because a stable eventId is passed (userId-derived), not the minute-bucketed default"
    - "purchaseCompleted / papPaymentCompleted helper signatures accept partnerClickId + isFirstPurchase so plan 11-02 can thread them without a type error"
  artifacts:
    - path: "pft-backend/src/app/modules/Tracking/tracking.interface.ts"
      provides: "partnerClickId + isFirstPurchase fields on ITrackingEventPayload"
      contains: "partnerClickId"
    - path: "pft-backend/src/app/modules/Tracking/tracking.events.service.ts"
      provides: "signupCompleted/purchaseCompleted/papPaymentCompleted arg types extended with partnerClickId (+ isFirstPurchase on purchase helpers); optional eventId passthrough"
      contains: "partnerClickId"
    - path: "pft-backend/src/app/modules/Auth/auth.service.ts"
      provides: "TrackingEvents.signupCompleted call at both registration-completion sites"
      contains: "signupCompleted"
  key_links:
    - from: "auth.service.ts one-step path (~:653)"
      to: "TrackingEvents.signupCompleted"
      via: "fire-and-forget call mirroring the CompleteRegistration FacebookPixel call, reading partnerClickId off the registered user doc"
      pattern: "TrackingEvents\\.signupCompleted"
    - from: "auth.service.ts two-step OTP path (~:1030)"
      to: "TrackingEvents.signupCompleted"
      via: "fire-and-forget call reading registeredUser.partnerClickId"
      pattern: "TrackingEvents\\.signupCompleted"
    - from: "tracking.events.service.ts fire() helper"
      to: "dispatch payload eventId"
      via: "optional eventId arg forwarded into the payload for stable dedup"
      pattern: "eventId"
---

<objective>
Extend the tracking event type surface with the fields Phase 11 needs (`partnerClickId`, `isFirstPurchase`, and an optional stable `eventId`), then wire the currently-ZERO-caller `TrackingEvents.signupCompleted()` at BOTH registration-completion sites in `auth.service.ts` so a partner registration postback (Phase 12) has an event to fire on.

Purpose: CRM-04. A partner clickid persisted on the User doc (Phase 10) must ride the signup_completed event so Phase 12's partnerPostback can attribute the registration. This plan also does the SHARED type extension that plan 11-02 depends on — it lands in Wave 1 so 11-02 can thread `partnerClickId`/`isFirstPurchase` without a compile error.

Output: `ITrackingEventPayload` gains `partnerClickId?` + `isFirstPurchase?`; the `signupCompleted`/`purchaseCompleted`/`papPaymentCompleted` helper arg types gain `partnerClickId` (+ `isFirstPurchase` on purchase helpers) and an optional `eventId` passthrough; two new `signupCompleted` call sites in `auth.service.ts`.
</objective>

<execution_context>
@/Users/klev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/klev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-wire-emits-dedup/11-RESEARCH.md

# Anchors verified 2026-07-01 against pft-backend @ local HEAD 4a079169:
@pft-backend/src/app/modules/Tracking/tracking.interface.ts
@pft-backend/src/app/modules/Tracking/tracking.events.service.ts
@pft-backend/src/app/modules/Tracking/tracking.service.ts
@pft-backend/src/app/modules/Auth/auth.service.ts
</context>

<pre_flight>
Repo hygiene (MEMORY.md — mandatory before any edit):
1. `cd pft-backend && git branch --show-current` MUST print `main-2026`. If not, STOP.
2. `git fetch origin main-2026 && git pull --rebase origin main-2026` — local HEAD 4a079169 is behind origin/main-2026 (72a5259). No remote drift on the three target files was detected at plan time, but re-check: `git log --oneline HEAD..origin/main-2026 -- src/app/modules/Tracking src/app/modules/Auth/auth.service.ts`. If remote already added `partnerClickId` to the tracking payload or a `signupCompleted` call site, DEFER to remote (adopt its shape, don't overwrite).
3. Scoped typecheck ONLY: `npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.events.service.ts`. Do NOT run full-project tsc (OOMs at 134). auth.service.ts is large — rely on CI for its full typecheck; scoped-check just the edited helpers.
</pre_flight>

<tasks>

<task type="auto">
  <name>Task 1: Extend tracking payload + helper signatures with partnerClickId, isFirstPurchase, and stable eventId passthrough</name>
  <files>pft-backend/src/app/modules/Tracking/tracking.interface.ts, pft-backend/src/app/modules/Tracking/tracking.events.service.ts</files>
  <action>
In `tracking.interface.ts`, in the `ITrackingEventPayload` interface (the "Domain-specific custom data" block, around line 220), add two optional fields:
  - `partnerClickId?: string;` — the partner S2S tracking click id (raw passthrough, byte-identical; Phase 12 encodes at send).
  - `isFirstPurchase?: boolean;` — FTD signal: true when this is the user's first completed purchase. See DESIGN NOTE below.

In `tracking.events.service.ts`:
  - Add `partnerClickId?: string;` to the `signupCompleted` arg type (currently `{ userId; email?; source? }`).
  - Add `partnerClickId?: string;` AND `isFirstPurchase?: boolean;` to BOTH `purchaseCompleted` and `papPaymentCompleted` arg types.
  - Add an optional stable-eventId passthrough so callers can force a deterministic dedup key. Give each of `signupCompleted`, `purchaseCompleted`, `papPaymentCompleted` an optional `eventId?: string;` field in their arg type, and make the internal `fire(eventName, args, req)` forward `args.eventId` into the payload when present. (The dispatcher at tracking.service.ts:169 already honors `payload.eventId` and only falls back to the minute-bucketed `deterministicEventId` when it's absent — so passing `eventId` gives durable cross-minute dedup. NO dispatcher change needed here; just make the helper NOT strip an eventId the caller supplies.) The current `fire()` already spreads `...args` into the payload, so `eventId` flows through automatically once it's an allowed field on the arg type — verify this and add an explicit test-of-eyes comment; do not remove the spread.

DESIGN NOTE — bake this comment into the code near `isFirstPurchase` so the plan-checker and future readers see it:
  `purchase_completed` / `pap_payment_completed` are SHARED multi-destination events (Meta CAPI, GA4, Klaviyo consume EVERY purchase). They are NOT suppressed on repeat purchases. FTD is expressed as the `isFirstPurchase` boolean flag ONLY. The "conversion postback fires once per user" guarantee is enforced in Phase 12 (partnerPostback gates the conversion send on `isFirstPurchase === true`). Phase 11 only PRODUCES the flag.

Do NOT touch any other helper, adapter, or the dispatcher body. Additive only.
  </action>
  <verify>
`cd pft-backend && npx tsc --noEmit --skipLibCheck src/app/modules/Tracking/tracking.interface.ts src/app/modules/Tracking/tracking.events.service.ts` exits 0.
`grep -n "partnerClickId" src/app/modules/Tracking/tracking.interface.ts` shows the new field.
`grep -n "isFirstPurchase\|partnerClickId\|eventId" src/app/modules/Tracking/tracking.events.service.ts` shows the extended arg types on signupCompleted/purchaseCompleted/papPaymentCompleted.
  </verify>
  <done>`ITrackingEventPayload` has `partnerClickId?` + `isFirstPurchase?`; the three helper arg types accept `partnerClickId`, `eventId`, and (purchase helpers) `isFirstPurchase`; scoped tsc passes; no existing helper behavior changed. Commit atomically to main-2026 + push: `git add -A && git commit -m "feat(11-01): extend tracking payload with partnerClickId/isFirstPurchase/eventId (CRM-04 groundwork)" && git push origin main-2026`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TrackingEvents.signupCompleted at both registration-completion sites with partnerClickId</name>
  <files>pft-backend/src/app/modules/Auth/auth.service.ts</files>
  <action>
Import the helper if not already imported: `import { TrackingEvents } from "../Tracking/tracking.events.service";` (check existing imports first; mirror the FacebookPixelService import style at line ~29).

Wire signup_completed at BOTH sites, immediately AFTER the existing `FacebookPixelService.trackStandardEvent("CompleteRegistration", ...)` call, mirroring its fire-and-forget shape (`.catch(...)` swallow):

SITE A — one-step (2FA-disabled) path, after the CompleteRegistration call at ~:649-653. The saved user doc is `createdUser`. Add:
  ```
  TrackingEvents.signupCompleted({
    userId: String(createdUser._id),
    email: normalizedEmail,
    eventId: `signup:${String(createdUser._id)}`,
    ...((createdUser as any)?.partnerClickId ? { partnerClickId: (createdUser as any).partnerClickId } : {}),
  });
  ```
  The `eventId` is a STABLE per-user key so a repeat registration/OTP submit dedups to one signup_completed (CRM-08 / SC#1). `signupCompleted` is fire-and-forget internally (it never throws) so no `.catch` is strictly required, but wrap it in a try/catch to match the file's defensive style if the surrounding code does.

SITE B — two-step OTP path, after the CompleteRegistration call at ~:1020-1030. The registered user doc is `registeredUser`. Add:
  ```
  TrackingEvents.signupCompleted({
    userId: String((registeredUser as any)?._id),
    email,
    eventId: `signup:${String((registeredUser as any)?._id)}`,
    ...((registeredUser as any)?.partnerClickId ? { partnerClickId: (registeredUser as any).partnerClickId } : {}),
  });
  ```

partnerClickId sourcing: `partnerClickId` lives on the User doc (Phase 10, CRM-02). Confirm the in-scope doc actually carries it:
  - One-step: `createdUser` is the full saved doc (`newUser.save()`), so `partnerClickId` is present.
  - OTP: `registeredUser` — verify it is the full user object (not a trimmed `.select()` projection like the `newUser` at :989). If `registeredUser` was loaded with a field projection that omits `partnerClickId`, either add `partnerClickId` to that `.select()` or read it from the full doc. Grep the OTP method for where `registeredUser` is assigned and confirm/fix the projection.

Skip-when-absent is mandatory: NEVER pass an empty-string partnerClickId (the `...(x ? { partnerClickId: x } : {})` spread handles this). Do NOT add a User.findById lookup — read from the doc already in scope.
  </action>
  <verify>
`grep -n "signupCompleted" src/app/modules/Auth/auth.service.ts` shows exactly TWO new call sites plus the import.
Confirm the OTP `registeredUser` carries partnerClickId: `grep -n "registeredUser" src/app/modules/Auth/auth.service.ts` — trace its assignment; if a `.select(...)` projection is present, confirm it includes partnerClickId or that the field was added.
Scoped compile of the tracking helper is already green from Task 1; auth.service.ts full typecheck deferred to CI (file too large for local full tsc). Sanity-grep for balanced syntax around both insertions.
  </verify>
  <done>Both registration-completion paths call `TrackingEvents.signupCompleted` with a stable `eventId` and a conditionally-included `partnerClickId`; the OTP path's user doc is confirmed to carry partnerClickId; skip-when-absent verified (no empty string possible). Commit atomically to main-2026 + push: `git add -A && git commit -m "feat(11-01): wire signup_completed at both registration sites w/ partnerClickId (CRM-04)" && git push origin main-2026`.</done>
</task>

</tasks>

<verification>
- `grep -n "partnerClickId\|isFirstPurchase" src/app/modules/Tracking/tracking.interface.ts` → both fields present on ITrackingEventPayload.
- `grep -c "signupCompleted" src/app/modules/Auth/auth.service.ts` → 3 (1 import reference-free + 2 call sites) or 2 call sites + 1 import line.
- Scoped tsc on the two Tracking files exits 0.
- No existing tracking helper or dispatcher behavior altered (additive only).
</verification>

<success_criteria>
- CRM-04 emit wiring in place: signup_completed fires once per registration on both paths, carrying partnerClickId when the user has one, with a stable userId-derived eventId enabling per-user dedup.
- Shared type extension (partnerClickId + isFirstPurchase + eventId passthrough) landed so plan 11-02 compiles.
- Skip-when-absent honored — no empty partnerClickId ever emitted.
- Both commits pushed to origin/main-2026.
</success_criteria>

<output>
After completion, create `.planning/phases/11-wire-emits-dedup/11-01-SUMMARY.md`. Note: (a) whether the OTP `registeredUser` needed a `.select()` fix for partnerClickId, (b) the exact final line numbers of both signupCompleted call sites (they shift after rebase), (c) confirmation that the `eventId` passthrough required no dispatcher change.
</output>
